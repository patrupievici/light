# Zvelt — Logic Optimization Audit

_Generated 2026-06-16 by a 13-partition multi-agent audit (98 agents). 85 findings, 82 confirmed real, 3 rejected._

Each finding was independently re-verified by a second agent that read the cited code. `behaviorPreserving:false` means the fix is real but a naive change would alter behavior — read the verifier note.


## Findings (sorted by severity)

### 1. [HIGH] [dead-code] StravaLabsTab is entirely dead code (709 lines)
- **File:** `app/lib/screens/analytics/strava_labs_tab.dart`
- **Where:** whole file — class StravaLabsTab
- **Est. reduction:** 709 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** A repo-wide grep for `StravaLabsTab` matches only this file's own declaration — it is never imported, pushed, or routed anywhere. The entire 709-line screen (5 fl_chart builders: _paceLine/_splitsBar/_elevationLine/_loadModelChart/_prCurve/_burnVsSteps, _hrZonesDonut, plus _PacePoint/_ManualSessionLite/_LoadPoint models and the CTL/ATL load model) loads health, stats and activity-store data but is unreachable. It also still pulls in fl_chart and duplicates the same FlTitlesData/FlGridData boilerplate found in analytics_hub_screen.dart.
- **Proposal:** Delete the file. If the experimental charts are wanted later they are in git history. This removes 709 LOC and one fl_chart consumer with zero behavior change.
- **Verifier note:** Confirmed dead: `StravaLabsTab` is referenced only inside its own file (grep) and the filename appears only in markdown docs (CLAUDE_DESIGN_PROMPT.md, QA_BACKLOG.md) — no Dart import, route, or instantiation. File is exactly 709 lines with all the cited chart builders, model classes, and CTL/ATL load model. Deletion is behavior-preserving. Estimate accurate; note that removing this one consumer does not drop the fl_chart dependency since other analytics screens still use it.

### 2. [HIGH] [dead-code] Entire onboarding_v2.dart is dead code (only a string constant is still used)
- **File:** `app/lib/screens/onboarding/onboarding_v2.dart`
- **Where:** whole file — OnboardingV2 (l.101) + _Step01.._Step24, _StepAITalk, _StepPermissions, _StepSource, _StepBody, _StepCommunity, 6 CustomPainters, _Profile/_pr/toPayload (l.560-3776)
- **Est. reduction:** ~3,750 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** main.dart instantiates only OnboardingV3 (main.dart:477). The single thing it imports from v2 is the constant `kOnboardingV2CompletedKey` (main.dart:37, used at l.351 for a legacy 'already onboarded' check). A repo-wide grep for `OnboardingV2(` finds only the constructor declaration itself — the widget and all 24 step classes are never built. That is ~3,750 lines of UI, 6 CustomPainters, a parallel _Profile→OnboardingPayload mapper, and a full duplicate permission-request implementation (_StepPermissions._requestOne, l.1869) being compiled and shipped for nothing. It also forces readers/auditors to wade through it and risks divergence (v2 still says 'forge'/Pro paywall comments that no longer reflect the live flow).
- **Proposal:** Delete onboarding_v2.dart. Move the one survivor, `const kOnboardingV2CompletedKey`, into a tiny constants file (or inline it in main.dart) so the legacy-completion check still works. The active OnboardingPayload/_pr logic already lives in OnboardingService + onboarding_v3.dart's _OnbData.toPayload, so nothing live depends on v2's mapper.
- **Verifier note:** Verified: onboarding_v2.dart (3776 LOC) is fully dead — the only live use is `kOnboardingV2CompletedKey` (main.dart:37 import, :351 legacy-onboarded check). A repo-wide grep finds `OnboardingV2(` only at the constructor declaration; the widget and all step/painter classes are never built. main.dart:477 builds OnboardingV3 exclusively, which has its own _OnbData.toPayload feeding OnboardingService.completeOnboarding; OnboardingPayload itself lives in onboarding_service.dart, not v2, so v2's _Profile/_pr/toPayload is a parallel unused mapper. The only other v2 references (biology_tab.dart:4, onboarding_service.dart:19) are doc comments, not code deps. Deleting the file and relocating the one constant preserves behavior (the stale doc comment in onboarding_service.dart:19 should be updated but is cosmetic). Severity high and ~3,750 LOC estimate are accurate.

### 3. [HIGH] [dead-code] Four of six skeleton tabs are dead code (~970 LOC)
- **File:** `app/lib/screens/skeleton/skeleton_ranks_tab.dart`
- **Where:** whole files: skeleton_ranks_tab.dart (708), skeleton_social_tab.dart (216), skeleton_workouts_tab.dart (32), skeleton_nutrition_tab.dart (25)
- **Est. reduction:** ~981 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** A project-wide grep for the public class names shows only Home and Profile are referenced from main_screen.dart (SkeletonHomeTab line 47, SkeletonProfileTab line 66). SkeletonRanksTab, SkeletonSocialTab, SkeletonWorkoutsTab and SkeletonNutritionTab have ZERO references outside their own files. The workouts/nutrition files even carry header comments stating 'Nu e folosit' and naming the real tabs (workouts_tab.dart / nutrition_tab.dart). The ranks (708 LOC) and social (216 LOC) skeletons are full, compiled, maintained-looking screens that nothing instantiates — every fix swept across 'all skeleton tabs' pays a cost on ~960 lines of unreachable UI plus their private DTO/service classes (_RanksService, _RanksMeResponse, _ExplainSheet, etc.).
- **Proposal:** Delete skeleton_ranks_tab.dart, skeleton_social_tab.dart, skeleton_workouts_tab.dart and skeleton_nutrition_tab.dart (confirm the live ranks/social screens elsewhere are the wired ones, which main_screen.dart implies). If any is intentionally kept as a reference fallback, move it out of the compiled tree (e.g. a /reference doc) so it stops being audited and reskinned.
- **Verifier note:** Verified: SkeletonRanksTab/SocialTab/WorkoutsTab/NutritionTab have zero instantiations outside their own files (only refs project-wide besides the files are 3 markdown docs). main_screen.dart wires only SkeletonHomeTab + SkeletonProfileTab; the other four use live screens (ProgressHubScreen, SocialPlusScreen, WorkoutsTab, NutritionTab) which all exist. Workouts/nutrition skeletons carry explicit 'Nu e folosit' comments naming the real tabs. Line counts exact (708+216+32+25=981); estReduction ~981 is accurate (prose '~970/960' slightly understates). Deletion preserves behavior — high severity confirmed.

### 4. [HIGH] [dead-code] ~500 lines of block-commented dead code in ai.ts
- **File:** `backend/src/routes/ai.ts`
- **Where:** lines 79-162 (moved helpers), 407-611 (removed /onboarding-plan body), 763-1147 (removed /weekly-plan body wrapped in `if (false)`)
- **Est. reduction:** ~500 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Three large blocks of dead code dominate the file. Lines 80-162 are a block comment retaining the old bodies of sanitizePromptInput/generateGoalAdvice/parseJsonFromModel (already moved to ../lib/ai-helpers). Lines 408-611 are the commented-out legacy /onboarding-plan handler (the live handler at 399-405 just returns 410 GONE). Lines 764-1147 are the entire legacy weekly-plan implementation wrapped in `if (false) { ... }` inside a /* */ comment. Of the file's 1148 lines, roughly 500 are dead. This buries the ~5 real handlers, makes the file ~3x longer than the logic warrants, and the duplicated prompt strings inside the dead blocks (the two copies of the elite/non-elite advice prompt at 91-118 and 537-568) invite accidental edits to the wrong copy.
- **Proposal:** Delete all three dead blocks. The helpers live in ai-helpers, the /onboarding-plan stub already returns 410, and weekly-plan is fully delegated to generateAndPersistWeeklyPlan. Git history preserves the old code if ever needed.
- **Verifier note:** Confirmed: three large dead blocks in backend/src/routes/ai.ts (80-162, 408-611, 764-1147), all enclosed in /* */ comments, totaling ~600 commented lines of a 1148-line file. Helpers (sanitizePromptInput/parseJsonFromModel/generateGoalAdvice) are exported from lib/ai-helpers and imported live at line 15; /onboarding-plan handler returns 410 at 399-405; /weekly-plan delegates to generateAndPersistWeeklyPlan (confirmed exported from services/weekly-plan.service). Deleting all three is purely cosmetic and cannot change behavior since everything is already commented out — even safer than described (the if(false) block is itself inside a comment, not live unreachable code). The ~500 LOC estimate is reasonable, arguably conservative.

### 5. [HIGH] [duplication] SocketException/Timeout/ClientException catch block copy-pasted 7× in SocialFeedService
- **File:** `C:\proiect\redesign\app\lib\services\social_feed_service.dart`
- **Where:** getFeedPage, getBookmarks, getPost, toggleLike, getComments, addComment, toggleBookmark (lines ~96-104, 157-165, 211-221, 240-250, 275-285, 316-330, 429-442)
- **Est. reduction:** ~70-90 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The identical 9-line block that runs the http call inside try{} and maps SocketException/TimeoutException/http.ClientException to SocialFeedException(isNetworkError:true) is repeated verbatim 7 times. Several methods additionally repeat a near-identical error-body decode (hidePost/reportPost/deletePost/createPost have the same try{jsonDecode}catch{debugPrint best-effort skip} 4×). That is ~70 lines of pure boilerplate, and any timeout/header/version change must be edited in 7 places. The sibling ModerationService in the same partition already solves this with a single private _send(method, uri, body) helper (moderation_service.dart:49-74) — SocialFeedService just never adopted it.
- **Proposal:** Add one private helper, e.g. `Future<http.Response> _send(String method, Uri uri, {Map<String,dynamic>? body})`, that builds headers, dispatches by method, applies the timeout, and maps the three transport exceptions to SocialFeedException — exactly like ModerationService._send. Add a second `_decodeErrMessage(String body)` for the 4× error-body decode. Each public method drops to status-check + parse. Behavior preserved.
- **Verifier note:** Confirmed: the 9-line SocketException/TimeoutException/ClientException catch block is verbatim in exactly 7 methods (getFeedPage, getBookmarks, getPost, toggleLike, getComments, addComment, toggleBookmark), and the error-body decode block is duplicated 4x (createPost, hidePost, reportPost, deletePost); ModerationService._send already models the helper. Refactor preserves behavior ONLY IF the helper takes a per-call timeout param: timeouts vary (most use AuthService.httpTimeout, toggleBookmark hardcodes 22s, createPost has NO timeout) and _headers() must stay outside the transport-catch (it throws 401 not a network error). Net reduction is closer to ~50-60 LOC than the cited 70-90 (that's gross boilerplate). Severity is overstated as high — it is clean, bug-free, well-documented boilerplate; medium is more accurate.

### 6. [HIGH] [duplication] AnalyticsHubScreen + ProgressionScreen are a stale v1 duplicate of the live ProgressHubScreen
- **File:** `app/lib/screens/analytics/analytics_hub_screen.dart`
- **Where:** AnalyticsHubScreen (970 LOC) + ProgressionScreen (749 LOC)
- **Est. reduction:** ~1700 LOC | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** main_screen.dart routes the analytics tab to the V2 ProgressHubScreen. AnalyticsHubScreen is reachable only from a single 'Analytics & charts' row in skeleton_profile_tab.dart, and ProgressionScreen is reachable ONLY from inside AnalyticsHubScreen (2 push sites) — they form an isolated v1 island. Both re-fetch the SAME endpoints the V2 hub/detail screens already render (getDailyTraining, getWeeklyEffort, getTopExercises, getRankLp, getWeeklySessions, nutrition history, steps, burned calories) and re-implement e1RM progression, weekly volume, top-exercises and rank-LP charts that TrainingMetricDetailScreen + StrengthAnalyticsScreen already provide. This is ~1700 LOC of parallel charting maintained in lockstep with the real screens.
- **Proposal:** Confirm with the owner, then retire the v1 island: point the profile-tab 'Analytics & charts' row at ProgressHubScreen (or the relevant TrainingMetric detail) and delete analytics_hub_screen.dart + progression_screen.dart. If a single chart there is uniquely valuable, lift just that widget into the V2 path.
- **Verifier note:** The v1 island is real: AnalyticsHubScreen (970 LOC) is reachable only from skeleton_profile_tab.dart:292, and ProgressionScreen (748 LOC) only from inside the hub (lines 126/157); the analytics tab uses ProgressHubScreen (main_screen.dart:53). ProgressionScreen's getMyProgressionHistory duplication is genuine (also consumed by StrengthAnalyticsScreen + TrainingMetricDetailScreen). BUT the proposed swap is NOT behavior-preserving: the hub embeds 5 chart widgets (VolumeProgressionChart, WorkoutConsistencyHeatmap, PersonalRecordsTimeline, MuscleBalanceChart, RestTimeTrendChart, ~1545 LOC in app/lib/widgets/charts/) that are used NOWHERE else — V2 ProgressHubScreen does not render them, so deleting the hub drops user-visible features (consistency heatmap, muscle balance, rest-time trend). Severity high is fair and the 2-file ~1718 LOC delete is accurate, but the proposal must port those 5 unique widgets into the V2 path (or consciously drop them) rather than treat the deletion as a no-op. Also note WeeklyEffortScreen, imported by the hub, has its own profile-tab entry (line 301) and must be kept.

### 7. [HIGH] [duplication] _SetLogDialog in tracker duplicates the shared SetLogDialog widget
- **File:** `app/lib/screens/workouts/workout_tracker_screen.dart`
- **Where:** _SetLogDialog / _SetLogDialogState, lines 364-528
- **Est. reduction:** ~164 LOC | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** The private _SetLogDialog (164 lines) is a near-verbatim copy of widgets/set_log_dialog.dart's public SetLogDialog: same SetLogMode switch, same weighted/bodyweightReps/timeSeconds slider blocks, same RPE row, same _submit tuple returns. The only differences are the tracker version uses kSetWeightMaxKg/kSetRepsMin constants and a slightly different time-slider cap. quick_launch_sheet.dart already imports and uses the shared SetLogDialog. Two copies means any fix to set-logging UX (e.g. the Decimal-string crash class noted in project memory, or bounds) must be made twice.
- **Proposal:** Delete _SetLogDialog/_SetLogDialogState from workout_tracker_screen.dart and route _showSetDialog through the shared widgets/set_log_dialog.dart SetLogDialog (already a dependency of this package). Reconcile the two constant differences (pass maxReps/weight cap as params, which SetLogDialog already accepts) so behavior is preserved.
- **Verifier note:** Duplication is real: _SetLogDialog (lines 364-528) is a near-verbatim copy of the shared SetLogDialog; weighted/reps/RPE/_submit blocks are byte-identical and the tracker's kSet* constants resolve to the same literals (0.0/500.0/1) the shared widget hardcodes. Severity high and ~164 LOC estimate are accurate. However, a straight swap is NOT behavior-preserving: the time-mode slider differs (tracker caps seconds at 500 with divisions:99; shared caps at 600 with divisions:119) and the _holdSeconds initState upper guard differs (500 vs 900). To preserve behavior the shared widget would need those time-slider params made configurable; once reconciled the consolidation is safe.

### 8. [HIGH] [perf] _OnbField rebuilds a TextEditingController on every build()
- **File:** `app/lib/screens/onboarding/onboarding_v3.dart`
- **Where:** _OnbField.build (l.797-799)
- **Est. reduction:** structural (1 controller/field instead of 1/build) | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** _OnbField is a StatelessWidget whose build() does `final ctrl = TextEditingController(text: value)..selection = ...` every frame. Because the parent _patch()/onChanged call setState on the onboarding root, every keystroke rebuilds the whole screen, allocating a fresh controller (never disposed → leak) and re-seeding selection. This widget backs every text input in the active flow: name, username, free-text goal, and age/height/weight on _ScrBiology (5+ live instances). Building a controller in build is an anti-pattern that wastes allocations on the hot input path and can fight the IME cursor.
- **Proposal:** Make _OnbField a StatefulWidget that owns one TextEditingController in initState (seeded from widget.value) and disposes it; only push value→controller in didUpdateWidget when they differ (guarding selection). Alternatively keep it stateless but drop the controller entirely and use TextField(initial... ) via a controller hoisted/owned by the parent screen. Eliminates per-keystroke allocations and the leak.
- **Verifier note:** Confirmed at onboarding_v3.dart l.797-799: _OnbField is a StatelessWidget that builds `TextEditingController(text: value)..selection=...` in build(). Each keystroke routes onChanged -> patch() (l.205-206) -> setState on the root, which rebuilds the active screen and reallocates an undisposed controller per frame. Verified 6 live call sites (name, username, goalText, age, height, weight at l.1336-1522), so the finding's "5+ instances" and leak claims hold. Severity high is slightly generous for raw perf since onboarding isn't a sustained hot path, but it's justified: besides the leak, re-seeding selection to collapsed(offset: value.length) every build forces the cursor to end-of-text, which would jump on mid-string edits. The proposed StatefulWidget (own controller in initState, sync only in didUpdateWidget when differing) is behavior-preserving and additionally fixes that latent cursor bug.

### 9. [HIGH] [redundant-io] _load() re-runs 4-5 network calls (incl. a full spinner) after every local micro-edit
- **File:** `app/lib/screens/nutrition/nutrition_tab.dart`
- **Where:** _load (81-160) called from onAdd(329), water(346), weight(371), onRemove(468), goals(654), patch(314)
- **Est. reduction:** ~4 requests + 1 full-screen rebuild per micro-edit | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** All mutation handlers call `await _load()` with the default showSpinner:true. _load runs syncHistoryFromServer(14 days) + Future.wait([getDay, getGoals, getWeeklyPlan, getMe]) every time, AND flips _loading=true so the entire tab flashes the full-screen CircularProgressIndicator (build line 396) for a state change that is purely local (e.g. +250ml water). It also re-evaluates the auto-bootstrap-week block. This is a heavy, multi-request rebuild for what should be an in-place setState.
- **Proposal:** For local mutations, update _day in setState directly from the value just written (entries/water/weight are all known at the call site) instead of calling _load(). If a refetch is desired, at minimum pass showSpinner:false and skip syncHistoryFromServer/getWeeklyPlan/getMe for non-plan edits. Reserve the full _load() for first open and pull-to-refresh.
- **Verifier note:** Confirmed at app/lib/screens/nutrition/nutrition_tab.dart: all 6 mutation handlers (onAdd 328, water 345, weight 371, onRemove 468, goals 653, patch 313) call `await _load()` with default showSpinner:true. _load (81-160) sets _loading=true (full-screen spinner at build line 396-397), runs syncHistoryFromServer(14d) + Future.wait of getDay/getGoals/getWeeklyPlan/getMe, and re-checks the auto-bootstrap block. getGoals and getMe BOTH hit GET /me, so it's ~4-5 requests (estimate accurate, if anything slightly understated). The proposal is behavior-preserving for water/weight/entry edits: nutrition_service.dart addEntry/updateWater/updateWeight/removeEntry (886-938) each build the exact `next` DailyNutrition locally and persist it, so the post-edit display state is fully known at the call site and identical to what getDay would reconcile (local dirty write wins, getDay 851-856). Severity high is justified — this fires on the app's most frequent actions and flashes a full-screen spinner for a purely local change.

### 10. [HIGH] [redundant-io] N+1 serial exercise lookups when bootstrapping a gym preset
- **File:** `app/lib/screens/workouts/quick_launch_sheet.dart`
- **Where:** _ActiveWorkoutViewState._bootstrapGymWorkout, lines 820-851
- **Est. reduction:** ~12 serial requests → 1-2 round-trips | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** For a non-existing-workout preset, the bootstrap loops over every preset exercise (6 for Push/Pull/Full Body) and awaits a separate getExercises(query: ex.name, limit: 20) network call inside the loop, then awaits addExercise per match. These run strictly sequentially (await inside `for`), so a 6-exercise preset fires 6 serial search round-trips + 6 serial add round-trips = 12 chained requests before the user can log a set. On a typical mobile RTT this is multiple seconds of countdown-to-live latency and is the dominant cost of starting a preset workout.
- **Proposal:** Resolve the catalog matches concurrently: build the list of name lookups and run them with Future.wait (the per-exercise try/catch can stay inside each future so one failure still degrades to skipped). Better, add/use a batch exercise-resolve endpoint or a single getExercises call that returns the catalog once (the library screen already fetches up to 300 in one call via getExercises(limit:300)) and match preset names against that single response in memory — eliminating the per-exercise search entirely. addExercise calls can likewise be issued via Future.wait. Preserves the exact match/skip semantics.
- **Verifier note:** The N+1 is real: quick_launch_sheet.dart lines 826-851 await getExercises(query, limit:20) then addExercise serially per preset exercise; largest presets (Push/Pull/Full Body) have 6 exercises = 12 chained round-trips before logging. Severity high is fair for cold-start latency. But the proposal is only PARTLY behavior-preserving. (a) Future.wait over the per-name getExercises calls is safe and equivalent. (b) addExercise via Future.wait is NOT safe as written: backend workouts.ts:247-253 auto-assigns position = lastPosition+1 via a read-then-write, so concurrent adds race and scramble/duplicate ordering; it must pass explicit position:index to preserve the current append order. (c) The single bulk getExercises(limit:300) match-in-memory variant is not strictly equivalent: the search endpoint filters by name-contains and orders by name asc (not relevance), so the current res.data.first fuzzy fallback would have to be re-implemented as in-memory contains, and a >300 catalog could miss matches a targeted query finds. Net: ~12→1-2 round-trips is achievable, but only the search half is a clean drop-in; the add-parallelization needs explicit positions and the in-memory-match needs fallback re-implementation.

### 11. [HIGH] [redundant-io] getSummary issues ~11 health reads strictly sequentially
- **File:** `app/lib/services/health_service.dart`
- **Where:** HealthService.getSummary (lines 374-501)
- **Est. reduction:** structural; ~10x latency on Health tab load | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** getSummary awaits each Health Connect/HealthKit read one after another: steps, calories, sleep, workouts, heart-rate, resting-HR (+ a conditional 36h HR fallback), SpO2, HRV, weight, lean-mass, body-fat — ~11-12 independent _readSafe / getTotalStepsInInterval calls in a row. Each is a plugin IPC/DB call that can take hundreds of ms (and up to the 30s _kCallTimeout used elsewhere) on cold caches. Total wall time is the SUM of all reads. None of them depend on each other except the resting-HR fallback (line 438) and the vo2 estimate (line 469), which only need their own inputs. This is the primary latency cost of opening the Health tab.
- **Proposal:** Group the independent reads into a single Future.wait: fire the steps + calories + sleep + workouts + hrToday + restingPoints + spo2 + hrv + weight + leanMass + bodyFat reads concurrently, then run only the resting==null fallback and the vo2 computation afterward. Behavior is identical (each read already swallows its own errors via _readSafe). On a device with many sources this cuts the tab load from ~11x to ~1x the slowest read.
- **Verifier note:** Confirmed: getSummary (lines 374-501) awaits ~11-12 independent reads strictly sequentially; only the resting==null fallback (438) and vo2 estimate (469) depend on prior results, so wrapping the rest in Future.wait is behavior-preserving (each _readSafe swallows its own errors). Two corrections: _readSafe does NOT apply _kCallTimeout (no 30s cap on these reads — that timeout is used elsewhere, 658-855), so the cited timeout claim is wrong; and the ~10x speedup is an optimistic upper bound since the health plugin may serialize calls over one platform channel. Severity high is fair.

### 12. [HIGH] [redundant-io] Every nutrition mutation does a network GET then the UI re-fetches the same day again
- **File:** `app/lib/services/nutrition_service.dart`
- **Where:** addEntry / removeEntry / updateWater / updateWeight (lines 886-938) + nutrition_tab callers (lines 314, 328, 346, 371, 468)
- **Est. reduction:** ~2 network GETs per food/water/weight action | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Each of addEntry/removeEntry/updateWater/updateWeight calls `getDay(date)` (line 889/903/917/931) to read the current day before writing. getDay() is network-first: it does a GET /nutrition/day, runs the dirty/reconcile branch, and may itself call _pushDayToServer (an extra PUT). After the write each method also calls _pushDayToServer (another PUT). Then the tab's onAdd/onRemove/onTap handlers immediately call `await _load()` which re-fetches the SAME day via getDay() yet again, plus syncHistoryFromServer, getGoals, getWeeklyPlan and getMe. Net per single water tap: getDay GET (inside updateWater) + PUT + then _load -> syncHistoryFromServer GET + getDay GET + getGoals GET(/me) + getWeeklyPlan GET + getMe GET. The day is fetched from the server 2-3 times for one local change.
- **Proposal:** Have the mutation methods read the local day via the in-memory/_loadDayFromPrefsOnly path instead of getDay() (the caller already holds _day in state and can pass it, or read prefs-only). Apply the mutation to the local copy, write prefs, push once, and have the handlers update _day via setState locally (or call _load(showSpinner:false)) without re-issuing a network getDay. This removes 1-2 full GETs per mutation.
- **Verifier note:** Verified: addEntry/removeEntry/updateWater/updateWeight (lines 886-938) each call the network-first getDay() (GET + possible reconcile PUT), then _pushDayToServer (PUT). Every tab handler (nutrition_tab.dart 313/328/345/371/468) then calls _load(), which runs syncHistoryFromServer (GET) + getDay() AGAIN (GET) + getGoals/getMe/getWeeklyPlan. Because the mutation's _pushDayToServer marks the day dirty, _load's getDay hits the dirty branch and fires a 3rd PUT — so /nutrition/day is touched ~2 GET + 2 PUT for one local change. A ready-made _loadDayFromPrefsOnly() already exists, so the proposed swap is straightforward and removes ~2 GETs per action. Behavior is preserved for the single-device offline-first model (prefs is canonical, dirty-wins reconcile still happens on next real read); only edge case is a minor multi-device freshness regression where the mutation would no longer pull server-newer data before applying its delta. Severity high is fair given this fires on every food/water/weight tap.

### 13. [HIGH] [redundant-io] getMe() never uses the existing saveMe/loadMe cache (dead cache + uncached hot path)
- **File:** `app/lib/services/profile_service.dart`
- **Where:** ProfileService.getMe (lines 39-49); AppDataCache.saveMe/loadMe (app_data_cache.dart 100-102)
- **Est. reduction:** ~4 redundant network calls per session; deletes 0 LOC but activates ~6 dead LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** AppDataCache exposes saveMe()/loadMe() (a per-user /me cache) but grep shows they are never called from anywhere in lib/ — only clearMe() is used (in clearSessionCaches). Meanwhile ProfileService.getMe() is called from 5 screens (nutrition_tab x2, progress_hub_screen, skeleton_profile_tab, xp_complete_screen) and always hits the network with no caching and no offline fallback. So the app pays a 45s-timeout-capable network round-trip for /me on every one of those screens, and the cache that was built for exactly this is dead code.
- **Proposal:** Make getMe() cache-first like getDailyQuote/getWorkoutSuggestion already do: add a refresh flag, return AppDataCache.loadMe() when present (within a short TTL via getTimed/putTimedJson), call saveMe() on 200, and fall back to loadMe() on error. This wires up the already-written cache, removes the dead code, and removes redundant /me fetches across screens.
- **Verifier note:** Verified: AppDataCache.saveMe/loadMe (app_data_cache.dart:100,102) are defined but never called anywhere in lib/ (only clearMe is used, in clearSessionCaches); getMe (profile_service.dart:39-49) does a raw uncached http.get with default withTimeout and is called from 5 sites (progress_hub_screen, xp_complete_screen, nutrition_tab x2, skeleton_profile_tab), so the dead cache + uncached hot path are both real, and getDailyQuote already proves the cache-first pattern in the same file. Behavior is preserved ONLY if the proposal's short-TTL + refresh-flag design is honored: getMe returns streak.currentStreak (read by skeleton_profile_tab), which is freshness-sensitive, so a naive permanent cache would show a stale streak. Severity is closer to medium than high: /me is a small fast read hit on navigation (not a tight loop) and 3 of 5 callers already wrap in catchError/Future.wait and degrade gracefully; the est of ~4 redundant calls/session and ~6 activated dead LOC is fair.

### 14. [HIGH] [redundant-io] getRecentProgression and computeProgressiveLoads redundantly re-query the same workout-history tables in one request
- **File:** `backend/src/lib/progression-context.ts`
- **Where:** getRecentProgression (progression-context.ts:31-150) + computeProgressiveLoads (progressive-overload.ts:59-188), called together in weekly-plan.service.ts:87 & :201 and routes/ai.ts:803 & :999
- **Est. reduction:** 2 DB queries removed per plan generation (x2 call sites) | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** In the weekly-plan and onboarding-plan paths both functions run for the SAME userId in the same request. getRecentProgression issues: (1) workoutSet.findMany(tag WORK, isCompleted, weightKg>0, last 90d, take 800), (2) userExerciseRank.findMany(in ids), (3) a second workoutSet.findMany(older sets, take 2000). computeProgressiveLoads then issues its OWN: (1) exercise.findMany, (2) workoutSet.findMany(tag WORK, isCompleted, weightKg>0, take 300) — overlapping the same rows getRecentProgression already pulled, (3) userExerciseRank.findMany(in ids) — the IDENTICAL query getRecentProgression already ran. So per plan generation there are ~5-6 DB round-trips against workoutSet/userExerciseRank, of which the rank query and a large slice of the WORK-set scan are duplicates. On the p95<800ms ranking/post SLO this is wasteful.
- **Proposal:** Have getRecentProgression return (or expose via a second value) the per-exercise lastSet map and the userExerciseRank map it already computed, and pass them into computeProgressiveLoads as optional pre-fetched inputs (it already builds lastSetByEx and rankByEx from the same predicates). Callers in weekly-plan.service.ts and ai.ts run them sequentially anyway, so threading the already-fetched data eliminates the duplicate userExerciseRank.findMany and the overlapping workoutSet scan — roughly 2 of ~6 queries per generation.
- **Verifier note:** Some overlap is real (both hit workoutSet + userExerciseRank for the same userId in one request), but the finding is materially wrong on specifics. (1) It claims computeProgressiveLoads runs once per request; it actually runs INSIDE the per-day for-loop (weekly-plan.service.ts:182/201, ai.ts:973/999), so it fires once per training day (3-6x), not once. The "~5-6 round-trips / remove 2" math undercounts and is inaccurate. (2) The rank query is NOT identical: getRecentProgression queries ranks for its top-12 recently-trained exercise IDs, while computeProgressiveLoads queries for the AI-prescribed IDs of THAT day — different `in (ids)` sets. (3) The proposed threading would change behavior: getRecentProgression only covers top-12 lifts trained in the last 90d and never fetches exercise.rankModel/movementPattern; prescribed exercises outside that set (and its WORK scan has a 90d floor that computeProgressiveLoads lacks) would wrongly fall to no_history. A safe optimization is possible (batch all days' ids into one call, or fetch exercise metadata once) but the finding's specific proposal is not behavior-preserving and the estimate is overstated. Severity is at most medium.

### 15. [HIGH] [redundant-io] Friend streaks endpoint is N+1 and over-fetches every workout per friend
- **File:** `backend/src/routes/friends.ts`
- **Where:** GET /v1/friends/streaks, lines 290-348 (Promise.all over friendIds)
- **Est. reduction:** N queries -> 1; bounded scan instead of full workout history | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** For each accepted friend, the handler issues a separate prisma.workout.findMany (lines 304-308) inside Promise.all — that is one query per friend (N+1: a user with 40 friends = 40 queries). Worse, each query pulls EVERY completed/posted workout that friend has ever logged with no date bound, then dedupes to distinct UTC days in JS (lines 309-311). A heavy user with thousands of workouts is fully materialized just to compute a consecutive-day streak that only needs the most recent ~contiguous run of days. Cost scales with friends x lifetime-workouts.
- **Proposal:** Replace the per-friend loop with a single grouped query: prisma.workout.groupBy({ by: ['userId','startedAt'... ]}) won't dedupe by day directly, so use one $queryRaw selecting DISTINCT (user_id, date_trunc('day', started_at)) for userId IN (friendIds) with status filter and a WHERE started_at >= now() - interval '40 days' (a streak longer than the bound is irrelevant for the UI). Bucket the rows into a Map<friendId, Set<dayStr>> in one pass, then run the existing streak walk per friend over that in-memory set. Turns N+1 unbounded queries into 1 bounded query.
- **Verifier note:** Confirmed: GET /v1/friends/streaks (lines 301-345) runs one prisma.workout.findMany per friend inside Promise.all with NO date bound (lines 304-308), fetching every completed/posted workout ever and deduping to day-strings in JS — genuine N+1 scaling as friends x lifetime-workouts. The proposal is sound and behavior-preserving: the streak walk (317-336) only consumes the most recent contiguous run, so a single bounded $queryRaw with DISTINCT date_trunc('day', started_at) for userId IN (friendIds) feeding an in-memory Set per friend yields identical results, except it caps streaks beyond the ~40-day bound (a deliberate, UI-acceptable trade, not a silent regression). The adjacent /activity endpoint (lines 362-369) already uses exactly this single groupBy-over-friendIds pattern, confirming feasibility. Severity high is justified.

### 16. [HIGH] [redundant-io] computeRanks does 2 sequential queries per exercise (N+1 read + N upserts)
- **File:** `backend/src/services/ranking.service.ts`
- **Where:** computeRanks, lines 233-272
- **Est. reduction:** ~2N DB round-trips → 2; structural | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** Inside the per-exercise loop (`for (const we of workout.exercises)`), each ranked exercise triggers a separate `prisma.userExerciseRank.findUnique` (line 248) followed by a separate `prisma.userExerciseRank.upsert` (line 258), all awaited sequentially. A workout with N ranked exercises performs 2N round-trips serialized one after another. The workout itself is already loaded with all exercises+sets in one query at line 217, so the only reason for the extra round-trips is the per-row prev/upsert. This is the hot post→ranking path with an 800ms p95 SLO.
- **Proposal:** Batch the reads: one `prisma.userExerciseRank.findMany({ where: { userId, exerciseId: { in: rankedExerciseIds } } })` into a Map before the loop, replacing the per-iteration findUnique. For writes, collect the upsert operations and run them via `prisma.$transaction([...upserts])` (or `Promise.all`) instead of awaiting each inside the loop. Reduces 2N serial round-trips to 1 read + 1 batched write.
- **Verifier note:** The 2N serial round-trips are real: ranking.service.ts line 248 (findUnique) and line 258 (upsert) are both awaited inside the per-exercise loop, while the workout+sets are already loaded once at line 217. But the proposal does NOT preserve behavior: schema (prisma/schema.prisma) has @@unique([workoutId, position]) on workout_exercises with NO unique on (workoutId, exerciseId), and the add-exercise route (workouts.ts:256) does not dedup, so the same exerciseId can appear twice in one workout. The current sequential code does a read-after-write per iteration, so a duplicate exercise's second occurrence sees the just-upserted lpTotal/strengthRatio; a pre-loop findMany Map plus batched $transaction/Promise.all would feed both iterations stale prev values and collide two upserts on the same PK, changing lpDelta, isAnomaly, and overallLpDelta. The batch is only safe if it first dedups by exerciseId (keep best occurrence), which the proposal omits. Also severity is overstated as high: N is small (a few ranked exercises per workout), so this is a modest savings, not a hot-loop blowup — medium is more accurate.

### 17. [MEDIUM] [complexity-tree] Recovery copy derived via a hand-rolled multi-branch ladder feeding 9 scalar fields
- **File:** `app/lib/screens/skeleton/skeleton_home_tab.dart`
- **Where:** _loadSteps (497-612), esp. the recovery/sleep/HRV/stress branches 536-609 and the 15-field State block 116-130
- **Est. reduction:** ~40 LOC + ~13 fields | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** _loadSteps computes recovery status/message/cta/aiRec via a 4-way if-ladder, then sleep (rating ternary + bar), HRV (rating ternary + bar), stress (rating ternary + bar) as separate inline blocks, writing into ~13 flat String/int State fields (_recoveryStatus, _recoveryMessage, _recoverySleepLabel, _recoverySleepRating, _recoverySleepBar, _recoveryHrvLabel/Rating/Bar, _recoveryStressValue/Rating/Bar ...). Those ~13 fields exist only to be re-passed verbatim into _syncWidgets (616-655) and a few vitals tiles. BodyMetricsService already owns the recovery math (compute() at 532) so the band->copy mapping is the only thing here, but it is open-coded as branches + flattened into a parallel field set that must be kept in sync in three places (compute, setState, _syncWidgets).
- **Proposal:** Move the band->(status,message,cta,aiRec) mapping into a small const table/record keyed by score band, and the sleep/HRV/stress label+rating+bar into 3 tiny pure helpers returning a record. Store the single computed BodyMetrics (already in _bodyMetrics) and derive labels on read, eliminating ~13 redundant State fields and the manual re-listing in _syncWidgets.
- **Verifier note:** Real but overstated. The 13 flat fields and their verbatim re-listing in _syncWidgets (635-647) are genuine boilerplate, and the if-ladder + 3 ternary blocks could be extracted into record-returning helpers. BUT the core justification is wrong: BodyMetricsService does NOT own this band->copy mapping. The tab uses different thresholds/strings (recovery 70/40 'Ready to train'/'Moderate'/'Rest day' vs service 75/50 'Primed'/'Fair recovery'/'Low recovery'; sleep hours-based 7.5/6.0 denom 9.0 vs service pct-based denom 8; HRV/stress ratings have no service equivalent). So 'store _bodyMetrics and derive labels on read' would change displayed copy/values — not behavior-preserving. Only 2 sync points exist, not 3 (compute is a separate mapping, not a copy). A safe extract-to-helpers refactor is possible but yields ~20-25 LOC and folds ~10 sync-only fields into 1 record, less than the '~40 LOC + ~13 fields' claimed. Severity should be low, not medium.

### 18. [MEDIUM] [dead-code] _TrendingRace wrapper is near-dead and _trending list is rebuilt every frame
- **File:** `app/lib/screens/social/race_hub_screen.dart`
- **Where:** _TrendingRace class (2101-2106), _challengeToTrending (86-95), _trending getter (97-98), card builder (516-602)
- **Est. reduction:** ~25 LOC + N allocations per frame | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** _TrendingRace carries three fields: title, colors, athletes. The athletes field is written ('$daysLeft days left', line 92) but never read anywhere (grep confirms the only other occurrence is the constructor) — the trending card shows '$count athletes' from _participantCounts and does not display days-left. So athletes is dead, and daysLeft is computed only to feed a dead field. title and colors are trivially derivable from _challenges[i] and _gradients[i % len] at the call site. Worse, the _trending getter maps the entire _challenges list into a fresh parallel List<_TrendingRace> on EVERY build() (and the itemBuilder indexes both _challenges[i] and _trending[i]), allocating N objects per rebuild for data already in _challenges.
- **Proposal:** Delete _TrendingRace, _challengeToTrending, _trending and the dead athletes/daysLeft computation. In the itemBuilder read challenge = _challenges[i] and colors = _gradients[i % _gradients.length] directly. Replace the _trending.isEmpty empty-state guard with _challenges.isEmpty.
- **Verifier note:** Verified: `athletes` is written (line 92) but never read (only occurrences are the constructor at 2102 and the write); `daysLeft` exists only to feed it. The `_trending` getter rebuilds a fresh List<_TrendingRace> on every access — and because it's a getter accessed inside the lazy itemBuilder (line 524), it re-allocates the whole N-length list per visible card, so the allocation cost is understated, not overstated. Card builder reads only r.title (==challenge.title) and r.colors (==_gradients[i % len]), both already derivable at the call site; _trending.isEmpty ≡ _challenges.isEmpty since it's a 1:1 map. Proposal preserves behavior. Severity medium and ~25 LOC estimate are fair.

### 19. [MEDIUM] [dead-code] Dead CustomPainter _MapRoutePainter (~96 lines) never used
- **File:** `app/lib/screens/workouts/quick_launch_sheet.dart`
- **Where:** _MapRoutePainter, lines 2040-2135
- **Est. reduction:** ~96 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** _MapRoutePainter is a fully implemented CustomPainter (grid background, dim/bright route path interpolation, mock street labels 'Oak St'/'Park Ave'/'River Rd') but is never instantiated anywhere. The live cardio view (_buildCardioLive, lines 1314+) renders a real FlutterMap with TileLayer/PolylineLayer instead. Grep across app/lib shows the only references to _MapRoutePainter are its own declaration and its shouldRepaint. It is leftover mock-map scaffolding from before the real map was wired in.
- **Proposal:** Delete the entire _MapRoutePainter class (lines 2040-2135). No call sites to update.
- **Verifier note:** Confirmed: _MapRoutePainter (lines 2040-2135, ~96 LOC) is never instantiated — grep shows only 3 self-references (constructor + shouldRepaint param) and the file contains zero CustomPaint usage. The live cardio view uses a real FlutterMap/TileLayer/PolylineLayer (lines 1330-1343), corroborating it's leftover mock scaffolding. Deletion preserves behavior; also remove the now-orphaned section comment at lines 2038-2039.

### 20. [MEDIUM] [duplication] login/signup/loginWithGoogle are three near-identical 28-line copies
- **File:** `C:\proiect\redesign\app\lib\services\auth_service.dart`
- **Where:** login (314-343), signup (345-383), loginWithGoogle (385-414)
- **Est. reduction:** ~45 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** All three methods share the same skeleton: build uri, http.post with Content-Type JSON + timeout, on non-expected-status decode body and throw Exception(message), else read accessToken/refreshToken from data and _saveTokens, with the identical `on FormatException { _authNetworkHint } catch (e) { if(_looksLikeNetworkFailure) _authNetworkHint; rethrow }` tail. Only the path, request body, expected status (200 vs 201) and the failure message differ. ~84 lines where ~35 would do; every auth-flow change (e.g. adding a header or capturing a new field) must be made three times.
- **Proposal:** Extract `Future<Map<String,dynamic>?> _authPost(String path, Map<String,dynamic> body, {int okStatus = 200, required String failLabel})` that does the POST, network-hint mapping, status check, token save, and returns data. login/signup/google become one-line delegations differing only by path/body/okStatus/label.
- **Verifier note:** Confirmed: login (314-343), signup (345-383), loginWithGoogle (385-414) share an identical skeleton (http.post + JSON header + timeout, _tryDecodeJsonObject-then-throw on bad status, jsonDecode + accessToken/refreshToken extraction + _saveTokens, and the identical FormatException/_looksLikeNetworkFailure/_authNetworkHint tail), differing only in path, body, expected status (200/201), and failure label. The proposed _authPost helper preserves behavior exactly; failLabel captures the only message difference. Estimate is sound — signup won't be a literal one-liner since it conditionally builds displayName, but body-building is already outside the duplicated block, so ~45 LOC net reduction holds. Severity medium is fair.

### 21. [MEDIUM] [duplication] FriendSummary row-mapping and label getter duplicated across FriendsService
- **File:** `C:\proiect\redesign\app\lib\services\friends_service.dart`
- **Where:** listFriends (73-83), searchByUsername (98-108), incomingRequests (125-137), outgoingRequests (150-162); label getters at 20-25 and 44-49
- **Est. reduction:** ~35 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The `list.map((e)=>e as Map).map((j)=>FriendSummary(userId:.., username:.., displayName:.., emailHint:..)).toList()` block is repeated 4× (FriendSummary twice, FriendRequestRow twice with createdAt added). FriendSummary and FriendRequestRow also carry byte-for-byte identical `label` getters (displayName → @username → emailHint → userId substring). incomingRequests/outgoingRequests differ only in the URL path.
- **Proposal:** Add `static FriendSummary FriendSummary.fromJson(Map)` and `FriendRequestRow.fromJson(Map)` like the other DTOs in this partition (e.g. DmPeer.fromJson). Hoist the shared label logic into one helper (free function or shared mixin) taking displayName/username/emailHint/userId. Collapse incoming/outgoing into one private `_requests(String path)`.
- **Verifier note:** Confirmed: the row-mapping block is repeated 4x (lines 73-83, 98-108, 125-137, 150-162) and the `label` getter is byte-for-byte identical in FriendSummary (20-25) and FriendRequestRow (44-49); incoming/outgoing differ only in URL path. The proposed fromJson pattern already exists in-partition (DmPeer.fromJson), so the refactor is idiomatic and behavior-preserving. Severity medium is right; ~35 LOC is the optimistic end (realistic net ~25-35).

### 22. [MEDIUM] [duplication] Network-failure detection and error-message extraction reimplemented per service
- **File:** `C:\proiect\redesign\app\lib\services\social_challenge_service.dart`
- **Where:** _looksLikeNetworkError (35-43); auth_service.dart _looksLikeNetworkFailure (24-35); ai_chat _throwIfError (38-48); integrations _extractMessage (175-185); plus the inline `j['message'] is String` extraction repeated 15× across social_challenge (8), social_feed (4), profile (2), planned_workouts (1)
- **Est. reduction:** ~40 LOC + correctness (unifies network-error classification) | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** Two cross-cutting primitives are hand-rolled in nearly every service: (1) a string-sniffing network-error predicate (auth's _looksLikeNetworkFailure and social_challenge's _looksLikeNetworkError are the same idea with slightly different keyword sets — a real divergence risk, e.g. auth checks 'clientexception'/'networkerror' but social_challenge doesn't), and (2) 'pull a user-facing message out of an error JSON body' which appears 15 times as inline try{jsonDecode; if(j['message'] is String)…}catch{best-effort skip}. http_client.dart already exists as the shared home for such helpers (it has authedJsonHeaders, withTimeout, friendlyLoadError) but these two were never added.
- **Proposal:** Add `bool isNetworkFailure(Object e)` and `String? errorMessageFrom(String body, {String fallback})` to http_client.dart (union of the keyword sets for the former). Replace the two private predicates and the 15 inline extractions with calls. Removes drift between services on what counts as 'network down'.
- **Verifier note:** Real duplication, well-grounded. Two divergent network predicates confirmed: auth's _looksLikeNetworkFailure (9 keywords incl. clientexception/networkerror/connection timed out) vs social_challenge's _looksLikeNetworkError (6 keywords) — genuine drift. http_client.dart is the established helper home (withTimeout/friendlyLoadError/authedJsonHeaders) and holds neither primitive. The named extractors (ai_chat _throwIfError, integrations _extractMessage) plus inline jsonDecode+message-pull sites are real, ~13-15 total — but the per-file breakdown is sloppy: social_feed has 0 message-extractions (its 'is String' hits are cursor/list checks, not error messages), social_challenge has 5 not 8, planned_workouts 2 not 1, profile 2. estReduction ~40 LOC is reasonable. NOT strictly behavior-preserving: unifying the predicate via keyword union BROADENS social_challenge's network detection (more failures fall back to local instead of rethrowing) — that's the intended fix but is a behavior change; and errorMessageFrom must accept both 'message'/'error' keys plus a per-call fallback to match the varied call sites. Severity medium is fair.

### 23. [MEDIUM] [duplication] Ten near-identical fl_chart builders in AnalyticsHubScreen
- **File:** `app/lib/screens/analytics/analytics_hub_screen.dart`
- **Where:** _dailyVolumeLine, _weeklySessionsBar, _weeklyEffortBar, _topExercisesBar, _rankLpBar, _nutritionMetricLine, _nutritionMetricLineInt, _bodyWeightLine, _stepsBar, _burnedCaloriesBar, _cardioBar (lines 399-969)
- **Est. reduction:** ~350 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Eleven chart methods (~570 LOC) repeat the exact same scaffolding: identical FlGridData(show:true, drawVerticalLine:false, getDrawingHorizontalLine: const FlLine(border,1)), identical topTitles/rightTitles 'showTitles:false', the same leftTitles 'k'-suffix formatter, and the same `idx<0||idx>=pts.length ? SizedBox.shrink()` bottom-label guard. Five bar builders differ only in color, bar width, the value accessor, and the bottom-label string. The maxY clamp+pad idiom (`reduce(max).clamp(...) * 1.1`) is copy-pasted ~10 times.
- **Proposal:** If the screen is kept, extract two helpers — `_barChart({required List points, required double Function value, required String Function label, Color, double width})` and `_lineChart(...)` — that own the shared FlTitlesData/FlGridData/border and the maxY computation. Each call site collapses to ~8 lines. (If the file is deleted per the previous finding, this is moot.)
- **Verifier note:** Real duplication: all 11 builders (lines 399-969) repeat identical topTitles/rightTitles showTitles:false, FlGridData(show:true,drawVerticalLine:false,FlLine border 1), borderData show:false, and the idx-bounds SizedBox.shrink guard — extractable into _barChart/_lineChart helpers. But the finding overstates specifics: there are 7 bar builders not 5; the claim 'differ only in color/width/accessor/label' is wrong (maxY formulas genuinely differ — int-fold vs double-fold vs reduce.clamp, floors 1/10/100/400/1000, multipliers 1.1/1.15/1.2, post-clamp fallbacks <1?5/<100?500/<10?30, plus reservedSize, k-suffix formatter, per-bar _pieColors indexing, and horizontalInterval), so the 'maxY idiom copy-pasted ~10 times' claim is false. _bodyWeightLine (span-padding minY/maxY, isCurved conditional, dots) and _dailyVolumeLine (area fill) don't fit one line helper cleanly. Behavior-preserving ONLY if every divergence is threaded as params/closures; realistic savings ~200-250 LOC, not ~350. Severity medium is fair (pure presentation, no bug).

### 24. [MEDIUM] [duplication] _SubHeader and _ZCard duplicated verbatim across two screens
- **File:** `app/lib/screens/analytics/hall_of_fame_screen.dart`
- **Where:** hall_of_fame_screen.dart _SubHeader (803-831) & _ZCard (783-801) vs strength_analytics_screen.dart _SubHeader (657-692) & _ZCard (696-718)
- **Est. reduction:** ~90 LOC | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** The `_SubHeader` widget (back button + title + trailing slot, safeTop padding) is byte-for-byte identical in hall_of_fame_screen.dart and strength_analytics_screen.dart. `_ZCard` is the same base (surface, radius 22, border, black26 shadow) — strength's variant only adds optional `accent`/`glow` flags. Both also duplicate the same orange-gradient retry button + cloud_off error state. Three screens (these two plus progress_hub's _IconBtn header) hand-roll the same sub-header chrome.
- **Proposal:** Promote one shared `AnalyticsSubHeader` and a single card widget (the project already has widgets/z/z_card.dart `ZCard` used by progress_hub) to the shared widgets layer; have hall_of_fame and strength_analytics import it. Fold strength's accent/glow into the shared card as optional params. Removes ~80 duplicated LOC and one more copy of the error/retry block.
- **Verifier note:** _SubHeader IS duplicated essentially verbatim across hall_of_fame (803-831) and strength_analytics (657-692) — extracting a shared AnalyticsSubHeader is real and behavior-preserving. _ZCard is also duplicated, with strength's adding optional accent/glow. BUT two claims in the finding are wrong/overstated: (1) the existing shared widgets/z/z_card.dart ZCard does NOT match the local _ZCard look — it has no border, default radius rLg (not 22), shadowCard (not black26/blur16), and antiAlias clip; folding _ZCard into it is NOT a drop-in and would change appearance unless border/radius/shadow params are added. (2) The error/retry blocks are NOT identical: hall_of_fame uses an amber gradient [FFB14A,D97706] while strength uses a red/orange [FF3D1F,FF7A2F], so naive dedup would alter colors. (3) progress_hub's _IconBtn is a 40x40 single-icon button, not a copy of the sub-header chrome — that 'third screen' claim is false. Severity medium is generous; ~40-50 LOC realistically removable (the two _SubHeader copies plus a parameterized card), not ~90.

### 25. [MEDIUM] [duplication] Two reveal overlays duplicate identical chrome helpers verbatim
- **File:** `app/lib/screens/onboarding/goal_advice_reveal_overlay.dart`
- **Where:** goal_advice_reveal_overlay.dart _botAvatar/_gradientWord/_cta (l.185-242) vs goal_interpret_overlay.dart _botAvatar/_gradientWord/_cta (l.204-261)
- **Est. reduction:** ~120 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** GoalAdviceRevealOverlay and GoalInterpretOverlay each define their own _botAvatar, _gradientWord and _cta as private methods that are byte-for-byte identical (same sizes, same 0x72FF5A1F shadow, same gradient, same disabled state). They also share the same PageRouteBuilder.route shape, the same Scaffold/SafeArea/Padding(22,20,22,12) scaffold, the same eyebrow row, and the same 'Skip' ghost button. ~120 lines are copy-pasted between the two files; any tweak (e.g. brand shadow) must be made twice.
- **Proposal:** Extract a small shared widget set — e.g. OnbBotAvatar, OnbGradientWord, OnbRevealCta — plus a `revealOverlayRoute()` helper into one file (these overlays already share the same idiom as onboarding_v2's _GT/_OnbCta, but those are dead). Both overlays then reduce to their unique header text + body content.
- **Verifier note:** Confirmed: _botAvatar, _gradientWord, and _cta are byte-for-byte identical between goal_advice_reveal_overlay.dart (l.185-242) and goal_interpret_overlay.dart (l.204-261), same 0x72FF5A1F shadow/gradBtn/disabled state, and both share the identical route PageRouteBuilder and Scaffold/SafeArea/Padding(22,20,22,12)/eyebrow/Skip shell. The helpers are pure (only ZveltTokens + params), so extraction is mechanical and behavior-preserving. Severity medium is fair, but the ~120 LOC figure is overstated — it counts both copies; net removable is ~80-90 LOC (the helper trio alone nets ~56). The 'onboarding_v2 _GT/_OnbCta are dead' aside is just a code comment, not load-bearing.

### 26. [MEDIUM] [duplication] Tier-from-LP mapping duplicated 3x in the ranks tab
- **File:** `app/lib/screens/skeleton/skeleton_ranks_tab.dart`
- **Where:** _ExerciseRankRow._tier (365-374), _ExplainSheetState._tierForLp (602-611), plus _tierColor switch (117-136) / _tierOrder list (115)
- **Est. reduction:** ~18 LOC + 1 source of truth | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** _ExerciseRankRow._tier and _ExplainSheetState._tierForLp are byte-for-byte the same 7-branch if-ladder (lp~/10 >= 600/500/.../100 -> tier name). Separately _tierColor is a 7-case switch and _tierOrder is the same 7 tiers as a list — three parallel encodings of one Iron→Olympian ladder. Any change to thresholds or tier names must be edited in three places, and this is inside an already-dead file, compounding finding #1.
- **Proposal:** Replace both if-ladders with one helper: define the ladder once as a const list of (threshold, name) pairs (or reuse _tierOrder with index = (lp~/10)~/100) and have a single tierForLp(int lp) return _tierOrder[(lp~/1000).clamp(0,6)]. Drive _tierColor from the same _tierOrder index instead of a parallel switch.
- **Verifier note:** Confirmed: _tier (365-374) and _tierForLp (602-611) are logically identical 7-branch ladders (both do lp~/10 then >=600/500/.../100 -> same tier names), and _tierColor (117-136) + _tierOrder (115) are two more parallel encodings of the same Iron->Olympian ladder. The proposed unification is behavior-preserving: index = (lp~/1000).clamp(0,6) reproduces every threshold exactly (lp=6000->6->Olympian, lp=999->0->Iron). The only edge is _tierColor's `default` branch becomes unreachable when driven by the clamped index, but it's unreachable for all valid inputs anyway. Grep confirms SkeletonRanksTab is never referenced outside this file, so the dead-file/finding-#1 compounding is also real. Severity medium and ~18 LOC estimate are accurate (arguably conservative).

### 27. [MEDIUM] [duplication] Story image-resolution chain duplicated between thumb and viewer
- **File:** `app/lib/screens/social/social_plus_screen.dart`
- **Where:** _StoryThumb (1291-1352) vs _StoryViewerScreenState._buildImage/_buildFileFallback/_buildBase64Fallback (2003-2047)
- **Est. reduction:** ~45 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The exact 3-tier fallback (network ZveltNetworkImage -> Image.file when imageFilePath exists -> base64 Image.memory -> placeholder), including the existsSync() guard, base64Decode try/catch, and reportError reasons, is implemented twice: once in _StoryThumb (build/_buildFileFallback/_buildBase64Fallback) and again in the viewer state (_buildImage/_buildFileFallback/_buildBase64Fallback). The only deltas are cacheWidth (storyThumb vs feedFull) and the placeholder widget (icon box vs black box). _hasAnyImage (1892-1897) also re-derives the same source-presence check the chain already encodes.
- **Proposal:** Extract a single reusable StorySourceImage widget (or top-level function) taking the SocialStory, a cacheWidth, and a placeholder builder, and use it from both _StoryThumb and the viewer. Derive _hasAnyImage from the same source-priority helper.
- **Verifier note:** Confirmed: the 3-tier network->file(existsSync)->base64(try/catch)->placeholder chain is duplicated verbatim in _StoryThumb (1298-1351) and the viewer (2003-2047), and _hasAnyImage (1892-1897) re-derives the same source-presence check. Real deltas are exactly cacheWidth (storyThumb vs feedFull) and placeholder (icon ColoredBox vs black ColoredBox), plus two minor points the refactor must parameterize to stay behavior-preserving: the reportError reason strings differ (image-file/base64-decode vs viewer-file/viewer-base64), and the viewer's Image.memory has NO cacheWidth while the thumb's does. Extraction into a StorySourceImage(story, cacheWidth, reasonPrefix, placeholderBuilder) is sound; ~45 LOC is realistic, maybe slightly optimistic after widget boilerplate. Severity medium is appropriate (UI fallback only, not correctness-critical).

### 28. [MEDIUM] [duplication] getDailyTraining and getCumulativeVolume re-implement the cache-first body instead of reusing the shared helper
- **File:** `app/lib/services/stats_charts_service.dart`
- **Where:** _cachedListData (174-201) vs getDailyTraining (221-248) and getCumulativeVolume (290-319)
- **Est. reduction:** ~30 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** _cachedListData already encapsulates the cache-first pattern (check getTimed within ttl, GET, putTimedJson, stale-fallback-on-error) and is reused by 5 getters. getDailyTraining and getCumulativeVolume hand-roll the exact same 25-line try/getTimed/putTimedJson/catch/rethrow flow because one returns from a custom map-shape and the other from {data:[]} at a different key. Three copies of the same control flow; any change to the caching policy (e.g. TTL, error semantics) must be made in three places.
- **Proposal:** Generalize the helper to a _cachedJson<T>(path, query, key, {required parse, ttl, refresh, isMap}) that returns the decoded payload and lets the caller map it (List for data-style, Map for cumulative). getDailyTraining then just maps via _dtFromList and getCumulativeVolume via _cvFromMap, both collapsing to ~5 lines. Behavior identical (same keys, TTLs, fallbacks).
- **Verifier note:** Confirmed: getDailyTraining (221-248) and getCumulativeVolume (290-319) re-implement the same cache-first flow as _cachedListData (174-201). getDailyTraining is a pure list-style case that could reuse the existing helper today with no generalization (just .map(DailyTrainingPoint.fromJson) after), so the duplication is if anything understated. getCumulativeVolume legitimately needs the proposed Map-shape generalization (checks c.value is Map and reads top-level year/totalKg, not {data:[]}). Keys, 2h TTLs, and stale-fallback/rethrow semantics all match, so a _cachedJson<T> with an isMap/type guard preserves behavior. Medium severity is fair; ~30 LOC is slightly optimistic (~20-30 net).

### 29. [MEDIUM] [duplication] Numeric-coercion helpers (toInt/toDouble/d) re-declared in ~9 fromJson bodies
- **File:** `app/lib/services/workout_service.dart`
- **Where:** WorkoutSetDto/WorkoutExerciseDto/ExerciseDto/WorkoutsMeta/SuggestedExerciseDto/PlannedExerciseDto/ExerciseRankDto/ProgressionPointDto/ExerciseProgressionDto fromJson (lines 855-1408); also stats_charts_service.dart
- **Est. reduction:** ~40-60 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Nearly every DTO redefines the same tiny num-coercion closures inline: toDouble (893), rest (855), fatigue (964), toInt (1067), toI/toD (1118-1123), toInt/toDouble (1268-1278), d (1331,1363,1395). These are byte-for-byte the same logic ('v is num ? v.toX() : parse(v.toString())') copied ~9 times in this file alone, and stats_charts_service repeats '(j[..] as num).toDouble()/.toInt()' across 6 DTOs. It is the single largest source of repeated code in the partition and each copy is a place a Decimal-as-string bug can creep back (per the project's known Prisma-Decimal pitfall).
- **Proposal:** Add top-level helpers once (e.g. in a json_coerce.dart or as static methods): num? asNum(v), double asDouble(v,[fallback]), int asInt(v,[fallback]), List<String> asStringList(v). Replace the per-DTO closures with calls to them. Pure refactor, identical parsing behavior, and it centralizes the Decimal-string handling.
- **Verifier note:** Confirmed: ~9 inline num-coercion closures are redefined across the DTOs in workout_service.dart (rest@855, toDouble/toInt@893-896, fatigue@964, toInt@1067, toI/toD@1118-1123, toInt/toDouble@1268-1278, d@1331/1363/1395), and stats_charts_service.dart repeats bare (j[..] as num).toX() casts across its DTOs. The duplication is real and medium-severity is fair. One correction to the finding: the copies are NOT byte-for-byte identical — they differ in null handling and fallback/error semantics (e.g. rest/fatigue/toDouble@1273 return null, toInt@896/toI use throwing int.parse, toInt@1268 takes a fallback param, toD@1119 returns 0.0, d@* return 0; stats_charts casts throw on string input). So a faithful refactor must expose a small family of helpers (asInt/asIntOr(fallback)/asNullableInt, asDouble/asDoubleOr/asNullableDouble) and map each call site to the matching variant to stay behavior-preserving; a single asInt/asDouble pair would change behavior. With that caveat the refactor is a pure, behavior-preserving consolidation. ~40-60 LOC estimate is plausible but likely toward the lower end (~30-50) given the extra variants needed; stats_charts adoption would intentionally make its parsing more lenient (Decimal-string safe) rather than identical, so leave those as a separate hardening step if strict parity is required.

### 30. [MEDIUM] [duplication] Chart loading/empty/error scaffold duplicated across 6+ chart widgets
- **File:** `app/lib/widgets/charts/volume_progression_chart.dart`
- **Where:** _buildLoading / _buildEmpty / _buildError in volume_progression_chart, muscle_balance_chart, personal_records_timeline, rest_time_trend, plus cumulative_volume_card & recent_prs_card inline variants
- **Est. reduction:** ~120-150 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Every chart re-implements the identical state-scaffold: a 20-24px-padded Container with ZveltTokens.surface bg, rLg radius, ZveltTokens.border border, and a centered CircularProgressIndicator(color: brand) for loading; the empty state is the same Container + 48px icon + bold title + secondary subtitle. _buildLoading is byte-for-byte identical in personal_records_timeline.dart (L100-112), muscle_balance_chart.dart (L96-108), volume_progression_chart.dart (L61-73) and rest_time_trend.dart (L45-57). The empty state is near-identical in 4 files (only icon/title/subtitle text differ). That is ~4 copies x ~25 lines of pure boilerplate plus the StatefulWidget load() try/catch which is itself near-identical (setState loading/error pattern repeated verbatim in 6 files).
- **Proposal:** Extract a single ChartCard scaffold widget (or ChartStateBuilder) that takes loading/error/empty/child and renders the shared Container decoration + loading spinner + empty template (icon/title/subtitle params). Optionally a small mixin or AsyncChartState<T> base that owns _loading/_error/_data and the setState try/catch so each chart only implements fetch() + buildChart(data). Collapses 4 identical _buildLoading bodies to 1 and 4 empty templates to 1.
- **Verifier note:** Real: _buildLoading is byte-for-byte identical in volume_progression, personal_records_timeline, muscle_balance, and rest_time_trend; the bordered empty-state scaffold (surface/rLg/border + 48px icon + bold title + secondary subtitle) is near-identical in the same 4. But the finding overstates scope: cumulative_volume_card and recent_prs_card use a DIFFERENT card (radius 16, no border, strokeWidth-2 inline spinners, header+body composition) and would not fold into the shared scaffold without visual change — so it's 4 files, not 6. Extracting a ChartCard/state template for the 4 is behavior-preserving; a shared AsyncChartState<T> load() base is riskier since the 4 load() bodies diverge (muscle_balance has no error state and a _zeroedCategories fallback, rest_time has no try/catch). Realistic reduction ~80-110 LOC, not 120-150. Severity medium is fair-to-slightly-high.

### 31. [MEDIUM] [duplication] Catmull-Rom curve smoothing + gradient area-fill painter logic duplicated 3x
- **File:** `app/lib/widgets/z/z_sparkline.dart`
- **Where:** _LineSparklinePainter.paint (z_sparkline.dart L70-125), _TrendPainter.paint (z_performance_trend.dart L186-239), and the simpler line builders in zvelt_charts.dart (_AreaPainter L94-163 / _LinePainter L475-523)
- **Est. reduction:** ~80-100 LOC + one canonical curve impl | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** The exact same Catmull-Rom control-point smoothing block appears verbatim in two painters: z_sparkline.dart L95-101 and z_performance_trend.dart L210-216 compute cp1/cp2 with the identical `(p2-p0)/6` and `(p3-p1)/6` math. The min/max normalization (`range = (maxV-minV).abs()<0.0001?1.0:...`) and the gradient area-fill (Path.from(path)..lineTo bottom..close, LinearGradient brand 0.22->0) are also copy-pasted across z_sparkline, z_performance_trend, and zvelt_charts' _AreaPainter/_LinePainter. Four hand-rolled line/area painters coexist with the fl_chart-based charts, all doing the same job.
- **Proposal:** Extract a shared helper, e.g. `Path buildSmoothPath(List<Offset> pts)` and `void paintAreaFill(Canvas, Path, Size, Color)` in one painter-utils file, and have all four painters call them. Longer term consider deleting the bespoke painters in zvelt_charts.dart/z_sparkline in favor of a single configurable sparkline painter, since ZveltMiniLine and ZSparkline render visually equivalent mini area-lines.
- **Verifier note:** Partially real but overstated. The verbatim Catmull-Rom control-point math IS duplicated, but only 2x (z_sparkline.dart L95-101 and z_performance_trend.dart L210-216 are identical), not 3x: zvelt_charts.dart's _AreaPainter (L142-143) and _LinePainter (L506-507) use straight lineTo segments with NO smoothing. Extracting a shared buildSmoothPath for those two painters is safe/behavior-preserving and worth doing. But the broader proposal (shared paintAreaFill across all four, or deleting bespoke painters) is NOT behavior-preserving: the four painters differ in gradient alpha (0.22/0.22/0.45/0.4), padding, plus zvelt_charts adds grid lines, progress-clip animation, and end-dot fade. Realistic safe reduction is ~30-50 LOC across the two smooth painters; the ~80-100 LOC figure requires the behavior-changing consolidation. Suggest severity low-to-medium.

### 32. [MEDIUM] [duplication] buildGoalGuidance and detectGoalIntents duplicate the same INTENTS pattern scan, run twice on the same text
- **File:** `backend/src/lib/goal-guidance.ts`
- **Where:** buildGoalGuidance (goal-guidance.ts:170-192) and detectGoalIntents (199-207); both invoked on the same goal string in ai-workout-suggestion.service.ts:228+311 and deterministic-workout.service.ts
- **Est. reduction:** ~30 redundant regex evaluations per AI suggestion request; -10 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Both functions loop INTENTS and run the identical `intent.patterns.some(p => p.test(text))` regex scan. buildGoalGuidance additionally re-implements dedup via a `seen` Set even though INTENTS labels are already unique, so the Set is dead weight. In ai-workout-suggestion.service.ts the same onboardingGoalText is scanned twice in one request (detectGoalIntents at line 228, buildGoalGuidance at 311) — every INTENTS regex (7 intents, ~4-5 patterns each, ~30 regexes) runs twice over the text.
- **Proposal:** Extract a single private `matchIntents(text): GoalIntent[]` helper that does the one scan; have detectGoalIntents return `matched.map(i=>i.label)` and buildGoalGuidance map the same list to guidance. Drop the redundant `seen` Set. Callers that need both labels and guidance can call matchIntents once. Behavior-identical (labels already unique).
- **Verifier note:** Duplication is real: buildGoalGuidance (goal-guidance.ts:176-183) and detectGoalIntents (203-205) run the identical INTENTS pattern scan, and the `seen` Set is genuinely dead (labels are unique and the loop iterates INTENTS one-per-label). The double-scan-per-request is real ONLY in ai-workout-suggestion.service.ts (lines 228 + 311); deterministic-workout.service.ts never imports buildGoalGuidance — it only calls detectGoalIntents (twice, at 61 and 86), so the finding's caller list is overstated. Behavior is preserved in practice, with one caveat the finder missed: line 228 scans raw onboardingGoalText while line 311 scans a trimmed/.slice(0,800) copy, so a single shared matchIntents call must pick one input and could differ on >800-char goals. Severity is overstated — the runtime cost is ~30 trivial regex tests on a short string once per request (negligible); the real value is the -10 LOC dead-Set cleanup. Low, not medium.

### 33. [MEDIUM] [duplication] Refresh-token issuance block copy-pasted 4 times in auth.ts
- **File:** `backend/src/routes/auth.ts`
- **Where:** signup (95-107), login (163-175), google (293-303), refresh (356-368)
- **Est. reduction:** ~40 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The 4-statement sequence — app.jwt.sign(...expiresIn JWT_EXPIRES_IN ?? '15m'), generateRefreshToken(), sha256 hash, compute expiresAt from REFRESH_TOKEN_EXPIRES_DAYS ?? 30, prisma.refreshToken.create — is duplicated verbatim in all four auth handlers (~12 lines each, ~48 lines total). The expiresAt-from-env computation alone appears 4x. Any change (token rotation policy, expiry default, adding device metadata) must be made in four places.
- **Proposal:** Add a single helper `issueSession(app, userId, email): Promise<{accessToken, refreshToken}>` that signs the JWT, generates+hashes the refresh token, computes expiry, and persists the RefreshToken row. Each handler then returns the result. The transactional user-creation in signup/google stays; only the token-minting tail is extracted.
- **Verifier note:** Confirmed: the JWT-sign + generateRefreshToken + sha256 hash + expiresAt-from-REFRESH_TOKEN_EXPIRES_DAYS + refreshToken.create tail is duplicated verbatim at signup (95-107), login (163-175), google (293-303), and refresh (356-368); only userId/email vary, so an issueSession(app,userId,email) helper cleanly subsumes all four and preserves behavior (token creation already runs outside the signup/google transaction, which the proposal keeps). Severity medium is fair; realistic net reduction is ~30 LOC, not ~40 (4x12 minus ~13-line helper and ~4 call-site lines).

### 34. [MEDIUM] [duplication] Accepted-friend-IDs query duplicated across 5 route files with no shared helper
- **File:** `backend/src/routes/challenges.ts`
- **Where:** challenges.ts:27-35 (acceptedFriendIds), friends.ts:23-30 / 292-295 / 353-356, stories.ts:57-60, messages.ts:11-22 (areFriends), posts.ts
- **Est. reduction:** ~35 LOC and 5 divergent copies collapsed to 1 | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The exact friendship lookup `findMany({ where: { status:'accepted', OR:[{userId:me},{friendUserId:me}] } })` followed by `.map(r => r.userId===me ? r.friendUserId : r.userId)` is reimplemented in challenges.ts (acceptedFriendIds), friends.ts (3 inline copies at lines 26, 293, 354), and stories.ts:57-60. messages.ts has a 4th variant (areFriends) doing the same OR-pair check. The friendIdFromRow/(r.userId===me?...) flip logic appears at least 6 times. Drift risk: any change to friendship semantics (e.g. blocking) must be edited in 5+ places.
- **Proposal:** Extract `acceptedFriendIds(me): Promise<string[]>` and `areFriends(a,b): Promise<boolean>` into a shared lib (e.g. src/lib/friendships.ts) and import everywhere. challenges.ts already has the canonical implementation; lift it and delete the four inline copies in friends.ts plus the stories.ts copy and messages.ts areFriends.
- **Verifier note:** Confirmed real. The accepted-friend-IDs query+flip is duplicated in challenges.ts:27-35, friends.ts:23-30/292-295/353-356, stories.ts:57-60, and also posts.ts:459-462 (and posts.ts:247-254 with an equivalent where-shape) — 6 list copies, not 5. The pairwise areFriends check exists in messages.ts:11-22 and inline in posts.ts:68-74. No src/lib/friendship helper exists. Extraction preserves behavior since all variants are semantically identical Prisma queries; estimate of ~35 LOC is reasonable, arguably slightly conservative. Severity medium is fair (pure maintainability/drift risk, no current bug).

### 35. [MEDIUM] [duplication] Visibility / UUID-validation gate duplicated across challenge handlers
- **File:** `backend/src/routes/challenges.ts`
- **Where:** join (174-193), participants (220-235), delete (433-458) vs loadVisibleChallenge (255-275)
- **Est. reduction:** ~45 LOC and 5 copies of the UUID guard -> 1 | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** A loadVisibleChallenge helper (255-275) already centralizes 'validate UUID -> findUnique -> 404 -> friends-visibility 403'. But join (174-193), participants (220-235), and delete (433-458) each re-implement the same UUID check + findUnique + 404 (and join/participants re-implement the friends-visibility block too) instead of calling it. The `if (!id || !z.string().uuid().safeParse(id).success)` 400 block alone is copy-pasted 5 times in this file (lines 177, 210, 223, 258, 436).
- **Proposal:** Route join and participants through loadVisibleChallenge (it already returns the row and handles the 403). For delete, add/extend a `loadOwnedChallenge` or reuse loadVisibleChallenge then check creatorId. Extract the UUID-or-400 guard into a tiny `requireUuidParam(request, reply)` helper to kill the 5 repeats.
- **Verifier note:** Confirmed: loadVisibleChallenge (255-275) centralizes UUID-400 + findUnique + 404 + friends-403, yet join (177-193) and participants (223-235) re-implement all of it, and delete (436-458) re-implements UUID+findUnique+404. The UUID guard is copy-pasted at exactly lines 177, 210, 223, 258, 436 (5x). A requireUuidParam helper (5->1) is fully behavior-preserving. Routing join/participants through loadVisibleChallenge is safe (join just adds its existing endsAt expiry check after, mirroring progress at 310-316) EXCEPT participants' 403 message would change from "Nu ai acces" to the helper's "Nu ai acces la această provocare" (trivial/improving). delete cannot reuse loadVisibleChallenge directly since it gates on creator ownership not visibility (different 403 message/path), so it needs the separate loadOwnedChallenge the proposal already calls out. Severity medium and ~45 LOC are fair (slightly optimistic).

### 36. [MEDIUM] [duplication] feedPostInclude defined but not reused; friend/hidden fetch duplicated
- **File:** `backend/src/routes/posts.ts`
- **Where:** feedPostInclude (45-59); GET /feed (266-306) and GET / (475-485) inline their own includes; friend+hidden fetch duplicated in /feed (247-263) and GET / (454-462)
- **Est. reduction:** ~30 LOC, removes 2 copies of friend/hidden logic | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The shared `feedPostInclude` constant is only used by GET /:id. GET /feed and GET / each hand-roll their own nearly identical include objects (user.profile select, privacySettings, workout->exercises->sets/exercise, _count likes/comments). Separately, the friendship query + friendIds mapping + postHide query appears verbatim in both GET /feed and GET / (the friendship where-clause and `f.userId === me ? f.friendUserId : f.userId` map are copy-pasted). Any change to feed shape or friend-visibility logic must be made in 2-3 places and they can silently drift (e.g. /feed include omits `visibility: 'public'` global which GET / adds).
- **Proposal:** Extract a `getFriendIdsAndHidden(userId)` helper returning { friendIds, hiddenIds } and reuse in both list endpoints. Reuse feedPostInclude (or a list-variant with the user select added) for the include in all three read endpoints.
- **Verifier note:** Confirmed: feedPostInclude is used only by GET /:id (line 318); GET /feed (280-305) inlines an identical workout/sets/_count include plus a user select; the friend+hidden fetch (postHide + friendship findMany + the `f.userId===me?...` map) is duplicated verbatim-ish in /feed (247-263) and GET / (454-462). A `getFriendIdsAndHidden` helper is safe and behavior-preserving. Two corrections: (1) GET /'s include (480-484) is NOT nearly identical — it intentionally OMITS the heavy `workout` relation since gallery/explore doesn't need it, so feedPostInclude should NOT be blindly reused there (only /feed can reuse it, with the user select added); the 'reuse in all three' framing overreaches. (2) The cited drift example (`visibility:'public'`) is in the WHERE clause and is an intentional feed-vs-explore semantic difference, not include drift. Severity medium is fair; ~30 LOC estimate is slightly optimistic (~20-25 realistic).

### 37. [MEDIUM] [duplication] Three near-duplicate JSON extractors for model output
- **File:** `backend/src/services/ai-workout-suggestion.service.ts`
- **Where:** parseLooseJson (20-34); meal-plan-ai.service.ts parseLooseJson (71-85); lib ai-helpers parseJsonFromModel
- **Est. reduction:** -~28 LOC; removes 2 of 3 copies | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** `parseLooseJson` is defined identically in ai-workout-suggestion.service.ts (lines 20-34) and meal-plan-ai.service.ts (lines 71-85) — same trim → JSON.parse → regex `/\{[\s\S]*\}/` fallback → JSON.parse logic, byte-for-byte. Meanwhile other services (weekly-plan, deterministic-workout, goal-aware-push) import `parseJsonFromModel` from lib/ai-helpers for the same job. Three implementations of one concern; the two local copies will drift from the shared one.
- **Proposal:** Delete both local `parseLooseJson` copies and import the shared `parseJsonFromModel` from lib/ai-helpers (adjust the generic typing call sites). If the regex fallback behavior differs, fold that into the single shared helper. One source of truth for lenient model-JSON parsing.
- **Verifier note:** Confirmed real: parseLooseJson in ai-workout-suggestion.service.ts (20-34) and meal-plan-ai.service.ts (71-85) are byte-for-byte identical to each other and to the live shared parseJsonFromModel in lib/ai-helpers.ts (22-36), which 5 other services already import. The only difference is typing: ai-workout-suggestion's copy is generic <T> (drop-in identical to parseJsonFromModel<T>); meal-plan's copy returns unknown|null, which parseJsonFromModel(out.text) reproduces via T=unknown inference — both call sites (lines 88/359 and 223) replace cleanly with no behavior change. (A 4th copy exists in routes/ai.ts:147 but it's inside a comment block, so the finding's count of 3 live impls is correct.) Severity medium is fair; estReduction ~28 LOC is accurate.

### 38. [MEDIUM] [perf] O(n^2) previous-completed-set lookup recomputed per set in build
- **File:** `app/lib/screens/workouts/workout_tracker_screen.dart`
- **Where:** _ExerciseCardState.build, lines 778-786
- **Est. reduction:** O(n^2)→O(n), ~3 list allocs/set removed | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Inside `...sortedSets.map((s){...})` the prevCompleted value is derived by filtering sortedSets THREE times for every set: once for the .isEmpty check, once for the .reduce, plus the where allocates intermediate iterables each time. For an exercise with n sets this is O(n^2) and allocates ~3 lists per set on every rebuild (and build runs every second via the elapsed timer setState). Since sets are already sorted by setIndex, the previous completed set is just the last completed entry before the current index.
- **Proposal:** Compute prev-completed in a single linear pass before the map: walk sortedSets once tracking the last completed set, storing each set's prevLabel in a local Map<String,String> (or iterate with an index and keep a running `WorkoutSetDto? lastCompleted`). Replace the triple-filter expression with a lookup. Behavior identical; O(n) instead of O(n^2) and far fewer allocations per rebuild.
- **Verifier note:** Confirmed: lines 779-786 filter sortedSets twice per set (.where for .isEmpty, then .where again for .reduce), making it O(n^2) with intermediate iterable allocations; sortedSets is sorted by setIndex (line 705) and parent's 1s timer setState rebuilds all inline _ExerciseCard children, re-running build each second. Single-pass last-completed tracking is behavior-identical. Severity is slightly overstated though — n is typically 3-5 sets/exercise, so absolute cost is small; closer to low/medium than medium.

### 39. [MEDIUM] [perf] Derived stats recomputed multiple times per rebuild in WorkoutsTab
- **File:** `app/lib/screens/workouts/workouts_tab.dart`
- **Where:** build/_buildCardioPanel/_buildStrengthPanel; _strainDelta (593-601), _muscleVolumes (609-629), _activityCounts (631-639), _eventsForDay (553-560)
- **Est. reduction:** 3-4x stat recomputations → 1; full-list aggregations off the hot rebuild path | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** build() calls _strainDelta() three times (lines 336, 338, 347) — each re-derives the average over _weeklyEffort — plus _strainDeltaLabel() calls it a 4th time. _eventsForDay is called for the selected day and again as the calendar eventLoader for every visible day. _muscleVolumes() iterates every workout × every exercise × every set, and _activityCounts() iterates all activity days, both recomputed on each rebuild even though the underlying lists only change on _load(). The tab rebuilds on every tab toggle and every setState. None of these results depend on transient UI state.
- **Proposal:** Compute the load-dependent aggregates once at the end of _load() (or in didUpdate when _workouts/_weeklyEffort/_activities change) and store them in fields: _strainDeltaCached, _muscleVolumesCached, _activityCountsCached. Reference the fields in build/_buildStrengthPanel/_buildCardioPanel. At minimum, hoist the single _strainDelta() result into a local at the top of build() instead of calling it 3-4 times.
- **Verifier note:** Redundancy is real: _strainDelta() runs 3x per build (line 336 via _strainDeltaLabel(), plus direct calls at 338 and 347 — the finding's "4th time" overcounts, it's 3 total), and _eventsForDay is called once for the selected day (247) and again per visible calendar cell via eventLoader (226). _muscleVolumes()/_activityCounts() are recomputed on rebuild but are gated behind _strengthTab==0 / _cardioTab==1, not unconditional. All inputs only change in _load(), so caching/hoisting preserves behavior. But every dataset is tiny (weeklyEffort<=6, dailyTraining<=30, activities<=30 days) and rebuilds are user-driven taps, not animation frames — real impact is negligible. Severity should be low, not medium; the cheap, safe win is hoisting _strainDelta() into one local at the top of build().

### 40. [MEDIUM] [perf] getDailyStepsHistory / getDailyCaloriesBurnedHistory loop one health read per day (up to 90 IPC calls)
- **File:** `app/lib/services/health_service.dart`
- **Where:** getDailyStepsHistory (513-531) and getDailyCaloriesBurnedHistory (552-579)
- **Est. reduction:** ~14-90 serial calls down to 1 (calories) / chunked (steps) | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** Both methods loop day-by-day (totalDays clamped to 1..90) and issue a separate awaited plugin call per day inside the loop: getTotalStepsInInterval per day, and getHealthDataFromTypes(TOTAL_CALORIES_BURNED) per day. For the 14-day default that's 14 serial IPC round-trips; for the 90-day max it's 90. This is an N-call serial pattern for data the platform can return in a single ranged query.
- **Proposal:** For calories, do ONE getHealthDataFromTypes over [todayStart-days .. now] and bucket points into days by dateFrom (a single O(n) pass), instead of N queries. For steps, if the plugin requires per-interval aggregation keep the calls but run them with bounded concurrency (e.g. Future.wait in chunks like backfillOnFirstGrant already does at chunkSize 3) rather than strictly serial. Same output, far fewer/parallel round-trips.
- **Verifier note:** Real: both methods loop 1..90 days with one awaited IPC call per day (steps L520-525 via getTotalStepsInInterval; calories L559-573 via getHealthDataFromTypes), so 14 default / 90 max serial round-trips. _numericFromPoint, dateFrom, and the chunkSize-3 Future.wait pattern (backfillOnFirstGrant L715-724) all exist as cited. The STEPS half (chunked concurrency, identical per-day calls) is behavior-preserving. The CALORIES single-query-then-bucket-by-dateFrom is NOT strictly equivalent: per-interval reads return records overlapping each window, so a record spanning a day boundary can be counted in two days (or in dateTo's day) by the current loop, whereas dateFrom-bucketing assigns it to exactly one day — totals can differ at boundaries. Medium severity is fair; this is chart/history read path, not a hot loop.

### 41. [MEDIUM] [perf] Heatmap rebuilds the full 365-day grid on every build() and re-allocates 365 date strings
- **File:** `app/lib/widgets/charts/workout_consistency_heatmap.dart`
- **Where:** _WorkoutConsistencyHeatmapState.build -> _buildHeatmap -> _buildWeeks (L130-310), _calculateStreaks (L96-122)
- **Est. reduction:** structural (eliminates per-frame 365-day recompute) | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** _buildHeatmap() calls `_buildWeeks(DateTime.now())` directly inside build(), and _buildWeeks walks all ~365 days (52 iterations x 7) allocating a `_ymd` string per day and building 52 record-typed week structures every single frame the widget rebuilds (e.g. on parent setState, scroll, theme change). The week layout depends only on _workoutDays + today (date), not on transient state, so this is pure recomputation in a hot path. Separately _calculateStreaks allocates 365 DateTime + 365 padded strings, but at least it runs once in _load.
- **Proposal:** Compute `_weeks` once in _load() (and when the day rolls over) and store it in a field, or memoize keyed by (today-ymd, _workoutDays identity). build() should just render the cached List. This removes 365 string allocations + 52 record allocations per rebuild.
- **Verifier note:** Confirmed: build()->_buildHeatmap() calls _buildWeeks(DateTime.now()) every rebuild, walking ~365 days, allocating a DateTime + _ymd string per day (L293) and ~53 week records — depends only on _workoutDays + today's date, so memoizing by (today-ymd, _workoutDays identity) is behavior-preserving. Severity 'medium' is slightly high: there are no animations/scroll-listeners driving this widget (horizontal SingleChildScrollView does not rebuild the child on scroll, and the only setState is in _load), so 'every frame'/'on scroll' is overstated — rebuilds come from external parent/theme changes. The allocation count and the fix are correct.

### 42. [MEDIUM] [redundant-io] Profile tab re-fetches /me and /workouts uncached, duplicating Home's cached calls
- **File:** `app/lib/screens/skeleton/skeleton_profile_tab.dart`
- **Where:** _loadProfile (72-107) via ProfileService.getMe(), _loadWorkoutCount (109-120)
- **Est. reduction:** 2 network GETs per Profile open | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** Home (_loadProfile line 417-447, _loadWorkouts 449-474) serves /me and /workouts?limit=1 from a 1h TTL cache (home_me_v1 / home_last_workout_v1). Profile tab fetches the exact same two endpoints with NO cache: ProfileService.getMe() (profile_service.dart 39-49) hits GET /me raw, and _loadWorkoutCount does GET /workouts?limit=1 raw — purely for meta.total, which Home already cached. Opening Profile right after Home re-pays both round trips, and Profile's own pull-to-refresh + post-settings _load() re-fire them every time.
- **Proposal:** Read /me from the same AppDataCache key Home uses (or route getMe through a TTL-cached service method), and drop _loadWorkoutCount in favor of the cached home_last_workout_v1 meta.total. At minimum give getMe() the same cache-first/getTimed treatment so the two tabs share one fetch.
- **Verifier note:** Duplication is real: Home caches GET /me + GET /workouts?limit=1 under home_me_v1/home_last_workout_v1 (1h TTL via getTimed, skeleton_home_tab.dart 417-474, keys 94-95), while Profile's getMe() (profile_service.dart 39-49) and _loadWorkoutCount (skeleton_profile_tab.dart 109-120) hit both endpoints uncached; the cited keys and ~2 GETs/open estimate are accurate. But the proposal as written is NOT behavior-preserving: Profile's _load() is intentionally re-fired after profile/units/physical-data edits (lines 407, 430, 476) to show freshly-saved data, and neither updateProfile nor the raw PATCH invalidates home_me_v1. Reading from the shared TTL cache there would display stale post-edit values (and leave Home stale too). A correct fix must add cache write-through/invalidation on the edit paths, so this is medium at most and not a clean drop-in.

### 43. [MEDIUM] [redundant-io] Full workout re-fetch after every single set log
- **File:** `app/lib/screens/workouts/workout_tracker_screen.dart`
- **Where:** _saveSetInline (180-207), _logPendingSet (148-178), _addSet (130-146)
- **Est. reduction:** ~1 full-workout GET per set logged | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Every set-completion path calls await _load() after the updateSet/addSet succeeds, which re-runs getWorkout(widget.workoutId) (full workout + all exercises + all sets) and rebuilds the whole tracker. During an active workout a user logs dozens of sets, so this is a GET-the-entire-workout request per set tap — pure redundant IO since updateSet/addSet already return (or could return) the updated set, and the local _workout model could be patched in place. It also resets all the per-set TextEditingControllers indirectly via rebuild and re-sends watch state.
- **Proposal:** Have updateSet/addSet return the mutated set/exercise (most of these service methods already return a DTO) and patch the in-memory _workout.exercises[..].sets entry, calling setState locally instead of _load(). Keep a single _load() on initial open and on pull-to-refresh / addExercise (where structure changes). Eliminates one full-workout GET per set.
- **Verifier note:** Confirmed: _addSet (141), _logPendingSet (173), _saveSetInline (197) each call await _load() after the mutation, and _load() (86-99) does a full GET /workouts/{id} (WorkoutDto with all exercises+sets) plus a loading-spinner setState. Both addSet and updateSet (workout_service.dart:173,204) already return the fully-populated WorkoutSetDto, so an in-place patch of _workout.exercises[..].sets is viable and the UI only renders fields present in that DTO (no server-derived rank/volume is shown mid-workout; ranking fires on post/complete). So the redundant full-GET-per-set is real; medium severity is fair (it's redundant IO, not a correctness bug, on a hot path). Two caveats that slightly correct the finding: (1) the 'controllers get reset by rebuild' claim is wrong — _kgCtrlFor/_repsCtrlFor cache by set.id via putIfAbsent (599-633), so rebuilds reuse them and ids are stable across patches, which actually makes the patch SAFER; (2) the proposal must keep a WatchConnectivityService.sendWorkoutState call after the local patch (line 99) or watch state goes stale — minor addition, not a blocker. Estimate of ~1 full-workout GET eliminated per set is accurate.

### 44. [MEDIUM] [redundant-io] GET /me fetched twice per nutrition load (getGoals + profileService.getMe), never cached
- **File:** `app/lib/services/nutrition_service.dart`
- **Where:** getGoals -> _fetchGoalsFromProfile (940-975) and nutrition_tab _load Future.wait (111-119)
- **Est. reduction:** 1 GET /me per load (and per mutation) | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** getGoals() unconditionally calls _fetchGoalsFromProfile() which does GET /me on every invocation (no cache window; prefs are only a fallback when the network fails). In the same _load(), the tab ALSO calls _profileService.getMe() in the Future.wait. So each load issues two independent GET /me requests that return the same profile object (one read for goals, one for bodyweight). getGoals is also called again on every mutation via _load.
- **Proposal:** Fetch /me once in _load and derive both NutritionGoals (via NutritionGoals.fromServerProfile) and bodyweight from that single response, or add a short-lived cache to the profile fetch so getGoals and getMe share it. Train/Progress already moved to cache-first; goals/profile should too.
- **Verifier note:** Confirmed real: nutrition_service._fetchGoalsFromProfile (lines 963-966) and profile_service.getMe (lines 43-46) both do an unconditional, uncached GET $v1Base/me, and nutrition_tab._load runs them concurrently in the same Future.wait (getGoals at 113, getMe at 116), so each load fires two parallel GET /me for the same profile object; getGoals also reruns on every mutation via _load. Both derivation helpers (NutritionGoals.fromServerProfile, _bodyweightFromProfileMap) operate on the same data['profile'] map, so a single-fetch refactor is feasible. estReduction of 1 GET /me per load/mutation is accurate. behaviorPreserving=true only if the refactor keeps getGoals' side effects: it writes goals to prefs on success (line 944) and falls back to cached prefs on network failure (948-956); a short-lived shared cache preserves this most safely. The signed-in bodyweight retry path (lines 129-136) is a distinct mutate+re-read and should stay. Severity medium is fair.

### 45. [MEDIUM] [redundant-io] N+1 query building post-workout insight prior comparison
- **File:** `backend/src/routes/workouts.ts`
- **Where:** GET /:id/insight, lines 483-515
- **Est. reduction:** N queries -> 1 (typically 6-8x fewer DB calls per insight) | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** For each exercise with logged sets, the code fires a separate prisma.workoutSet.findMany inside Promise.all (one round-trip per exercise). A workout with 6-8 exercises means 6-8 parallel queries every time the XP screen is opened (mitigated only after the 1h cache fills). Each query scans the same workout_sets table filtered to the same userId with status completed, differing only by exerciseId.
- **Proposal:** Replace the per-exercise loop with a single findMany over all exercise IDs (`workoutExercise: { exerciseId: { in: exerciseIds }, workout: {...} }`, tag WORK, completed, id != current workout) selecting weightKg/reps/exerciseId, then compute per-exercise max client-side via a Map keyed by exerciseId. Collapses N queries to 1.
- **Verifier note:** The N+1 is real: lines 483-515 fire one prisma.workoutSet.findMany per exercise (6-8 typical), all on workout_sets filtered by userId/status=completed/tag=WORK, differing only by exerciseId. But the proposal as written is NOT behavior-preserving: each per-exercise query uses orderBy createdAt desc + take:10, so it computes the max over only the 10 most-recent prior WORK sets. A single findMany with exerciseId:{in:[...]} cannot reproduce a per-exercise take:10 (Prisma take is global), so collapsing to one query+Map would compute max over ALL prior sets, changing output whenever an exercise's all-time max sits outside its last 10 sets. A correct single-query fix must over-fetch and re-apply per-exercise top-10 windowing client-side. Severity is closer to low: the calls run in parallel (Promise.all, so latency ~1 query not N) and a 1h in-process cache shields revisits; benefit is DB-call/load reduction, not user latency.

### 46. [MEDIUM] [redundant-io] checkAndAward issues several avoidable extra queries; streak query over-fetches
- **File:** `backend/src/services/achievement.service.ts`
- **Where:** checkAndAward (68-138) + getWorkoutDayStreak (19-42) + isUserInSeasonTop10 (44-57)
- **Est. reduction:** 3 serial round-trips → 1 parallel batch | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** After the initial Promise.all (4 queries), checkAndAward fires more queries sequentially: `workoutExercise.groupBy` (108), `getWorkoutDayStreak` (114) which findMany's up to 400 workouts (a list the function already counts in the Promise.all via workout.count but can't reuse), and `isUserInSeasonTop10` (120) which always runs 2 queries (season + top10) even though the season lookup duplicates the same active-season pattern used in ranking.service. These run on every completed workout. The groupBy, streak findMany, and season-top10 are independent and could be parallelized; getWorkoutDayStreak pulls 400 rows just to reduce to distinct UTC days.
- **Proposal:** Run the post-Promise.all independent reads (groupBy distinctExercises, getWorkoutDayStreak, isUserInSeasonTop10) inside a single Promise.all instead of sequentially. For the streak, select only `startedAt` (already done) but consider a `distinct`/groupBy on the day or a smaller take; better, fold the streak workout fetch into the first Promise.all batch so it's not a separate serial round-trip.
- **Verifier note:** Confirmed: after the 4-query Promise.all, checkAndAward awaits groupBy (108), getWorkoutDayStreak (114, findMany take:400 deduped in JS), and isUserInSeasonTop10 (120, 2 queries) sequentially with no inter-dependency — all keyed only on userId. Wrapping them in one Promise.all is behavior-preserving since the check() calls reading their results are unchanged. The active-season findFirst is indeed duplicated in ranking.service.ts (292-296). Estimate (3 serial round-trips collapsed to 1 parallel batch) and medium severity are accurate; runs on every completed workout.

### 47. [MEDIUM] [redundant-io] loadPushContext re-fetches workout timezone the batch query already selected
- **File:** `backend/src/services/goal-aware-push.service.ts`
- **Where:** runGoalAwarePushBatch (130-181) + loadPushContext/userTodayYmd (217-342)
- **Est. reduction:** -1 DB query per eligible user/run | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The batch query already selects each user's most-recent `workouts[0].timezone` (lines 144-150) and uses it for the hour gate (line 161). Then for users passing the gate, `loadPushContext(u.id)` runs `userTodayYmd(userId)` which issues ANOTHER `prisma.workout.findFirst` for the exact same most-recent workout's timezone (lines 332-337). The timezone value is already in hand from the outer query, so this is a redundant per-eligible-user query. Additionally loadPushContext re-queries the planned workout that the outer `where` already filtered on.
- **Proposal:** Pass the already-known tzOffsetMin (or u.workouts[0].timezone) from the loop into loadPushContext so userTodayYmd doesn't re-query. Keep userTodayYmd only for the standalone `sendGoalAwarePushForUser` path. Saves one workout query per user who reaches the hour window.
- **Verifier note:** Confirmed: the batch query selects each candidate's most-recent workouts[0].timezone (lines 145-150, used at 161), and for users passing the hour gate, loadPushContext->userTodayYmd issues an identical prisma.workout.findFirst for the same most-recent workout's timezone (lines 333-337). Passing the known offset in eliminates one query per eligible user; since the offset is derived from the same timezone string via the same extractTzOffsetMinutes, the computed day is identical, so behavior is preserved (keep userTodayYmd for the standalone sendGoalAwarePushForUser path). Severity medium is generous: this only fires for users in the single matching local-push hour, so volume per run is small. The finding's secondary claim that loadPushContext redundantly re-queries the planned workout is WRONG — the outer where uses day:{contains:''} (intentionally permissive per the comment at 174-176) and loadPushContext narrows to the specific local day, which is necessary work, not redundancy.

### 48. [MEDIUM] [redundant-io] getStreakStatus fetches lastPost twice and duplicates day-diff math
- **File:** `backend/src/services/streak.service.ts`
- **Where:** getStreakStatus (53-79) + updateStreak (5-51)
- **Est. reduction:** -1 DB query per call; ~10 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** `getStreakStatus` calls `prisma.post.findFirst` for lastPost (line 58), then calls `updateStreak` (line 72) which immediately re-runs the SAME `prisma.post.findFirst` for lastPost (line 10) plus a `findMany` of 100 posts. So lastPost is queried twice per call. The daysDiff/grace computation is also duplicated across both functions (lines 21-23 vs 69-70). For a read endpoint (300ms p95 SLO) this is a wasted query on every status read.
- **Proposal:** Have `getStreakStatus` compute everything from the single `findMany` it needs: fetch the recent posts once (descending), derive lastPost = posts[0], daysSinceLast, the streak loop, and isAtRisk in one pass. Either inline the streak computation or refactor `updateStreak` to accept the already-fetched posts so the lastPost query isn't repeated.
- **Verifier note:** Confirmed: getStreakStatus calls post.findFirst (line 58) then updateStreak re-runs the identical findFirst (line 10) plus a findMany (line 29) — lastPost is queried twice, and the daysDiff/grace math is duplicated (lines 21/47 vs 69/70). Proposal is behavior-preserving IF it keeps getStreakStatus's empty-case return of currentStreak:0 (updateStreak returns 1 in its no-post branch, but that branch is unreachable here since line 63 short-circuits first). Severity is closer to low-medium than medium: these are single-user, userId-indexed queries that are cheap, so the saved query is real but low-impact; LOC/query estimate is fair.

### 49. [LOW] [complexity-tree] Three parallel race-chat stores with overlapping responsibilities
- **File:** `C:\proiect\redesign\app\lib\services\race_chat_service.dart`
- **Where:** race_chat_service.dart (whole file, local SQLCipher store); race_chat_store.dart (separate store); social_challenge_service.dart getRaceMessages/sendRaceMessage (294-334, server-backed)
- **Est. reduction:** structural (up to ~110 LOC if one store is dead) | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Race chat is implemented three ways in this partition: RaceChatService (local-only SQLCipher table, explicitly 'PRIVATE · ONLY YOU', no network), a separate race_chat_store.dart (111 LOC), and SocialChallengeService.getRaceMessages/sendRaceMessage which hit `/v1/challenges/:id/messages` server-side. The local RaceChatService header documents that v1.0 is strictly local while the SocialChallengeService methods already send/read shared messages — the two models contradict each other and a caller could pick the wrong one. This is latent complexity/maintenance risk rather than a hot-path cost.
- **Proposal:** Confirm which store is the live one for the shipped race-chat UI and delete the unused path (or fold race_chat_store.dart and race_chat_service.dart into one). At minimum add a short doc note in each pointing to the other so the local-only vs server-backed split is intentional and discoverable. No behavior change for the surviving path.
- **Verifier note:** Real, and stronger than stated: the live race-hub UI (race_hub_screen.dart) uses ONLY the server-backed SocialChallengeService.getRaceMessages/sendRaceMessage; both RaceChatService (race_chat_service.dart, 175 LOC) and RaceChatStore (race_chat_store.dart, 111 LOC) have zero importers anywhere in the repo — they are dead code, not merely overlapping. Deleting both is behavior-preserving. Estimate understated: ~286 LOC removable, not ~110. Severity low is fair (latent maintenance/privacy-doc risk, no hot-path cost); just preserve the 'PRIVATE · ONLY YOU' no-upload design note somewhere if v1.1 chat is still planned.

### 50. [LOW] [complexity-tree] Muscle-group categorization is a hardcoded keyword if/else chain
- **File:** `app/lib/widgets/charts/muscle_balance_chart.dart`
- **Where:** _MuscleBalanceChartState._load, the name.contains(...) chain L50-68
- **Est. reduction:** ~15 LOC + extensibility | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Exercise->muscle-group bucketing is a 4-branch if/else where each branch ORs 3-4 `name.contains(...)` substring checks (Push/Pull/Legs/Core). It is hard to extend, duplicates the category keys already defined in _zeroedCategories(), and silently drops any exercise that matches none. The same Push/Pull/Legs/Core key list is written twice (the static _zeroedCategories map L19-24 and the inline categories map L39-44).
- **Proposal:** Drive it from a table: `const Map<String,List<String>> kGroupKeywords = {'Push':[...], 'Pull':[...], ...}` and loop `for (group,keywords) { if keywords.any(name.contains) { add; break; } }`. Build the zeroed map from the same table's keys so there is one source of truth. Flattens the branch chain and de-dupes the category list.
- **Verifier note:** Confirmed: the 4-branch if/else contains.contains chain (L50-68) and the duplicated Push/Pull/Legs/Core key map (L19-24 and L39-44) both exist exactly as described; the list is actually written THREE times (also L119 labels), so the finder slightly undercounted the duplication. The table-driven rewrite preserves behavior only if iteration order stays Push-first (Dart const maps keep insertion order), which preserves an existing quirk where 'leg press' buckets as Push (matches 'press' first) and L62's 'leg press' keyword is effectively dead. Severity low and ~15 LOC estimate are fair.

### 51. [LOW] [complexity-tree] Per-handler ExerciseDB proxy routes are near-identical and could be table-driven
- **File:** `backend/src/routes/exercises.ts`
- **Where:** GET /db/* handlers, lines 161-198 (8 handlers)
- **Est. reduction:** ~20 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Eight /db/* GET handlers (bodyparts, equipment, targets, bodypart/:name, target/:name, equipment/:name, name/:name, /db/:id) are structurally identical: parse optional :name, call exdbFetch with a templated path, send { data }. The four `/db/<list>/:name` handlers differ only by the path segment and all do `encodeURIComponent(name)`. This is ~38 lines of boilerplate for what is one parametric mapping.
- **Proposal:** Keep the list endpoints as-is (they're trivial) but collapse the four `:name` proxies into a loop over a small array of [routeSuffix, upstreamSegment] tuples that registers each handler, or a single `/db/by/:facet/:name` route with a whitelist Set of allowed facets. Either removes ~half the boilerplate while preserving identical behavior and the EXDB cache.
- **Verifier note:** Confirmed: the four /db/<facet>/:name handlers (lines 176-198) are genuinely identical except for the upstream path segment, so a tuple-driven loop registering each route would preserve behavior and the exdbFetch cache. But the scope/estimate is overstated: the title's "8 handlers" includes /db/:id (different param :id and upstream path /exercises/exercise/) and /db/image/:id (unauthenticated, image cache, separate logic) which are NOT collapsible; only 4 handlers (~16 LOC) qualify, and a loop nets ~9-10 LOC saved, not ~20. Severity "low" is correct.

### 52. [LOW] [dead-code] Dead/unused XP stub model fields and methods retained
- **File:** `app/lib/services/nutrition_service.dart`
- **Where:** NutritionXpClaimResult/NutritionXpClaimLine (539-578), claimDayXp (590-597)
- **Est. reduction:** ~60 lines | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** claimDayXp() is a permanent stub returning a fixed 'already claimed, 0 XP' result, and the comment at nutrition_tab line 437-441 confirms the only consumer (NutritionXpClaimCard) was removed. The two model classes carry ~10 fields (breakdown alias to lines, ageMultiplier, bonusApplied, pctOfTarget, hit, macro...) that nothing reads. This is ~60 lines of inert API surface kept 'for build compatibility' with a widget that no longer exists.
- **Proposal:** Confirm via grep that no widget references these types, then delete claimDayXp and both classes (or reduce to a tracked TODO). If kept for a near-term re-wire, trim to the fields the future endpoint will actually return.
- **Verifier note:** Confirmed: claimDayXp() (nutrition_service.dart:590-597) is a permanent stub returning fixed 0-XP/alreadyClaimed; its only former consumer (NutritionXpClaimCard) was removed per the comment at nutrition_tab.dart:437-441. Grep across the repo shows no widget/test reads the two model classes or calls the method, so deleting all three (~60 lines, 535-597) is behavior-neutral. Severity low and the ~60-line estimate are accurate. Sole caveat: a doc comment intentionally keeps it for a planned re-wire, so converting to a tracked TODO is a defensible alternative to deletion.

### 53. [LOW] [dead-code] _logRankError is a needless indirection that drops the StackTrace it receives
- **File:** `app/lib/services/workout_service.dart`
- **Where:** getMyRanks catch (597-605)
- **Est. reduction:** ~3 LOC + restores crash reporting | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** getMyRanks catches (e, st) and calls _logRankError(e, st), but _logRankError ignores st entirely and just does debugPrint('[...] $e'). The comment above getMyRanks claims failures log to Crashlytics, but this path never calls reportError — so the wrapper adds a layer, captures a StackTrace, and throws it away. It is both dead indirection and a missed crash report.
- **Proposal:** Delete _logRankError and inline reportError(e, st, reason: 'workout:get-ranks') (as every other catch in the partition does), or at minimum inline the debugPrint. Removes a method and makes the logging match the documented intent.
- **Verifier note:** Confirmed: _logRankError (defined line 603, used once at line 598) takes (e, st) but discards st and only debugPrints, while the comment at 584-585 claims failures log to Crashlytics; reportError is the established pattern in this file (line 577) and across 20 services. The 'inline debugPrint' variant is behavior-preserving, but the recommended 'inline reportError' variant changes behavior by starting crash reporting (which the finding openly states as 'restores crash reporting'). Severity low and ~3 LOC estimate are accurate.

### 54. [LOW] [dead-code] No-op preHandler hook on AI routes
- **File:** `backend/src/routes/ai.ts`
- **Where:** aiRoutes, lines 170-173
- **Est. reduction:** 4 LOC + one hook call per request | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** An `app.addHook('preHandler', async (request, reply) => { ... })` is registered whose body contains only comments — it does nothing but adds a hook invocation to every AI request. The comment even says the real limiting is done by per-route `config.rateLimit`, which is indeed set on each handler.
- **Proposal:** Delete the hook entirely. Per-route rateLimit config already provides the tighter AI limits.
- **Verifier note:** Confirmed at backend/src/routes/ai.ts lines 170-173: the app-level preHandler hook body is comments only — a true no-op that never mutates request/reply or short-circuits, so deleting it preserves behavior. All AI handlers carry per-route config.rateLimit (max 3-20); the only route lacking it (/onboarding-plan) is a deprecated 410 stub doing no AI work. Severity low is correct; the 'one hook call per request' saving is negligible runtime — this is dead-code cleanup, not a perf win.

### 55. [LOW] [duplication] Thousands-separator and month-name formatters re-implemented in 3-4 places
- **File:** `app/lib/screens/analytics/progress_hub_screen.dart`
- **Where:** _thousands (progress_hub 1372-1381, training_metric_detail 162-171), _fmt (hall_of_fame 450-458); month-name arrays in progress_hub _trendXLabels(516)/_weekLabels(484), training_metric_detail _weekLabel(557), strength_analytics _prDate(102)/_historyLabels(125)/_weekLabels(143), photo_progress _fmtDate(393)
- **Est. reduction:** ~50 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The same digit-grouping loop appears three times (progress_hub `_thousands`, training_metric_detail `_thousands`, hall_of_fame `_fmt` — the only difference is iterating front-to-back vs back-to-front). The `['Jan','Feb',...]` (and 'M','T','W'... weekday) arrays plus the `DateTime.tryParse(ymd) -> 'Mon D'` formatter are re-declared in at least 6 functions across 4 files.
- **Proposal:** Add a tiny shared formatters util (e.g. analytics/_format.dart or extend an existing util): `groupThousands(int)`, `monthDay(String ymd)`, `weekdayInitial(DateTime)`. Replace the inline copies. Pure refactor, no behavior change.
- **Verifier note:** Confirmed: the thousands-separator helper is triplicated with identical output (progress_hub_screen.dart:1372 and training_metric_detail_screen.dart:162 are char-identical; hall_of_fame_screen.dart:450 `_fmt` is a reversed-loop variant, same output) and consolidates cleanly with zero behavior change. The `['Jan'..'Dec']` array is duplicated across ~6 sites and weekday `['M','T',..]` across 3, so a shared util is warranted (utils/ dir exists as a home). However the finding overstates the date part: the formatters produce FOUR different outputs (`Jan 5`, `5 Jan`, `5/Jan`, `Jan 5, 2026`), so a single `monthDay()->'Mon D'` only cleanly replaces 2 of the 6 — full consolidation needs multiple helpers. Net reduction is realistically ~25-35 LOC, not ~50. Severity low is correct.

### 56. [LOW] [duplication] RHR->VO2 synthetic mapping formula duplicated in two biology helpers
- **File:** `app/lib/screens/biology/biology_tab.dart`
- **Where:** _buildVo2Trend (108-119) and _computeThirtyDayDelta (121-141)
- **Est. reduction:** structural (one source of truth for the curve) | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The exact magic-number mapping `(52.0 - (r.value!.clamp(42.0,90.0) - 42.0) * 0.42).clamp(24.0,64.0)` appears verbatim in both _buildVo2Trend (line 113) and _computeThirtyDayDelta (line 134). Any tuning of the proxy curve must be edited in two places or the trend line and the 30-day delta silently diverge.
- **Proposal:** Extract a single `double _rhrToVo2(double rhr)` helper and call it from both.
- **Verifier note:** Confirmed: the exact mapping `(52.0 - (r.value!.clamp(42.0,90.0) - 42.0) * 0.42).clamp(24.0,64.0)` appears verbatim at lines 113-114 (_buildVo2Trend) and 134-135 (_computeThirtyDayDelta). Both apply it to a non-null double (r.value!), so extracting `double _rhrToVo2(double rhr)` is a pure mechanical refactor with identical behavior. Severity "low" is accurate — both call sites are in the same class/file and the curve is currently consistent, so this is preventive (avoid future divergence) rather than a present bug.

### 57. [LOW] [duplication] Meal label + portion-unit pluralization logic duplicated across model and widgets
- **File:** `app/lib/screens/nutrition/nutrition_tab.dart`
- **Where:** _MealSection._label/_emoji (1341-1358) vs NutritionPlannedMeal.mealLabel (407-421); formatUnitCount switch in nutrition_food_labels.dart (41-52)
- **Est. reduction:** ~25 lines, removes a label-drift bug | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** The breakfast/lunch/dinner/snack -> label mapping exists in two independent switch statements: NutritionPlannedMeal.mealLabel (service) and _MealSection._label (tab). They already disagree ('Snack' vs 'Snacks'). Separately, formatUnitCount uses a 5-branch switch where every branch is the identical `whole && r==1 ? '1 X' : '$disp Xs'` shape — a singular/plural noun table would collapse it.
- **Proposal:** Centralize the meal-key->label map in one place (e.g. a const map on NutritionPlannedMeal) and reuse it from _MealSection. Replace the formatUnitCount switch with a `{key: (singular, plural)}` lookup and one format line.
- **Verifier note:** Duplication is real. The meal-key->label switch exists independently in NutritionPlannedMeal.mealLabel (services/nutrition_service.dart:407-421, NOT nutrition_tab.dart as cited) and _MealSection._label (nutrition_tab.dart:1341-1348), and for the same key 'snack' (passed at nutrition_tab.dart:461-463) they genuinely disagree: mealLabel returns 'Snack', _label returns 'Snacks'. A third capitalize-only variant exists at progress_hub_screen.dart:1215. formatUnitCount (nutrition_food_labels.dart:41-52) is a 5-branch switch where every branch is the identical `whole && r==1 ? '1 X' : '$disp Xs'` shape with all-regular plurals, so a singular/plural table collapses it cleanly. Centralization is feasible since nutrition_tab.dart already imports both files. Caveat: NOT strictly behavior-preserving — unifying 'snack' necessarily changes the displayed string at one of the two sites (that's the intended bug fix), and any map must keep mealLabel's empty/unknown-key fallback ('Meal' / capitalize). Severity low and ~25-line estimate are fair.

### 58. [LOW] [duplication] Bordered checklist/summary row list is hand-rolled three times
- **File:** `app/lib/screens/onboarding/onboarding_v3.dart`
- **Where:** _ScrBuilding rows (onboarding_v3.dart l.1744-1797) + _ScrSummary rows (onboarding_v3_screens.dart l.1778-1821) + _ScrNotifications rows (onboarding_v3_screens.dart l.1430-1467)
- **Est. reduction:** ~80 LOC | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** Three screens render the same pattern: a _Card containing a Column that maps an indexed list of rows, each row a Container with `border: i < len-1 ? Border(bottom: BorderSide(color: border)) : null`, EdgeInsets.symmetric(vertical: ~13), an icon/tile + title + sub. The divider-on-all-but-last logic, vertical padding, and title/sub typography are repeated nearly identically in all three, so the visual contract isn't enforced in one place.
- **Proposal:** Add a single `OnbRowList` (or `_DividedRows`) helper that takes a list of (icon, title, sub, optional trailing) and renders the bordered rows once, then have _ScrBuilding, _ScrSummary, and _ScrNotifications feed it data. Collapses three ~50-line builders into one helper + three data lists.
- **Verifier note:** The shared part is real but small: only the _Card+Column+Container divider wrapper (border-on-all-but-last, EdgeInsets.symmetric padding) is identical across all three. The row interiors diverge significantly — _ScrBuilding uses a stateful animated checklist (22px circle indicator tied to _done, Opacity wrapper, plain-string rows, conditional '···' trailing), _ScrSummary uses a 42px _TileIcon + title 14.5/sub 12 + fixed green check (5-tuples), _ScrNotifications uses an 18px brand Icon + title 13.5/sub 11 + no trailing (3-tuples). Padding also differs (13 vs 14) and sub font size differs (11 vs 12). A single (icon,title,sub,trailing) data-driven helper can't model the _ScrBuilding progress/opacity/string-row case without losing fidelity, so it wouldn't be behavior-preserving as proposed. A thinner helper that wraps ONLY the divided-Container/Column scaffold (taking a List<Widget> of row contents) is viable and behavior-preserving but saves far less — realistically ~25-35 LOC, not ~80. Severity low is correct; estimate is overstated.

### 59. [LOW] [duplication] _loadProfile and _loadWorkouts repeat identical cache-then-fetch boilerplate
- **File:** `app/lib/screens/skeleton/skeleton_home_tab.dart`
- **Where:** _loadProfile (417-447) and _loadWorkouts (449-474)
- **Est. reduction:** ~20 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Both methods are the same 18-line shape: if(!refresh) read AppDataCache.getTimed(key) with the same `age < 1h && value is Map` guard, else getAccessToken / http.get / statusCode!=200 bail / jsonDecode / putTimedJson. Only the cache key, URL and the post-parse field assignment differ. The 1h TTL and the Map-guard are copy-pasted, so a change to caching policy must be made twice.
- **Proposal:** Extract a single helper Future<Map<String,dynamic>?> _cachedGet({required String key, required String path, bool refresh}) that does the cache-guard + token + GET + putTimedJson and returns the decoded map; have both callers just map the result into their fields. This also makes the shared TTL a single constant.
- **Verifier note:** Confirmed: _loadProfile (417-447) and _loadWorkouts (449-474) share byte-identical cache-then-fetch boilerplate (getTimed + age<1h + value is Map guard, token bail, GET, statusCode!=200 bail, jsonDecode, putTimedJson), differing only in cache key, URL, and post-parse field mapping. Grep confirms exactly these two callsites; _loadTrainingHistory/_loadQuote use a different service-layer path, so scope is just these two. The proposed _cachedGet helper preserves behavior (the interleaved !mounted guards are identical in both and move cleanly into the helper). Net reduction is closer to ~10-13 LOC than 20 once the helper signature/body is counted; severity low is correct.

### 60. [LOW] [duplication] Workout date/duration formatting duplicated across helpers
- **File:** `app/lib/screens/skeleton/skeleton_home_tab.dart`
- **Where:** _lastWorkoutTimeLabel (669-685) + _lastWorkoutDurationMinutes (687-699) on the State, vs _LastWorkoutCard._formatDate (1988-2001) + _formatDuration (2003-2014)
- **Est. reduction:** ~25 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The State exposes _lastWorkoutTimeLabel/_lastWorkoutDurationMinutes (Today/Yesterday/d/m relative label + minutes diff) for the home widget sync, and _LastWorkoutCard re-implements the same Today/Yesterday/date and minutes-diff parsing in _formatDate/_formatDuration for the card UI. Same DateTime.parse + difference.inDays/inMinutes try/catch logic written twice from the same _lastWorkout map.
- **Proposal:** Put one set of pure formatters (relativeDay(iso), durationMinutes(start,end)) in a shared place and use them from both the widget-sync path and the card; the card already receives the workout map so it can call the same functions.
- **Verifier note:** Confirmed real low-severity duplication: _lastWorkoutTimeLabel (669-685) and _LastWorkoutCard._formatDate (1988-2001) share identical DateTime.parse+toLocal+inDays Today/Yesterday logic, differing only in fallback ('d/m' vs 'd/m/y'); _lastWorkoutDurationMinutes (687-699) and _formatDuration (2003-2014) share identical inMinutes parse, differing only in return type (int? vs '${m}min' string). Extraction is behavior-preserving via durationMinutes(start,end)->int? (card wraps with 'min') and relativeDay(iso,{withYear}). Note: a shared util already exists (app/lib/utils/relative_time.dart) but its 'Nm/Nh/Nd ago' cadence does NOT match the Today/Yesterday format, so it is not a drop-in; the fix is a new pair of formatters local to the home flow. Est ~25 LOC is slightly optimistic — callsite adapters (year flag, int->string wrap) mean realistic savings ~15-20 LOC.

### 61. [LOW] [duplication] Repeated 'Exception: ' stripping + snackbar boilerplate
- **File:** `app/lib/screens/social/race_hub_screen.dart`
- **Where:** race_hub: lines 264-266, 311-313, 758-760, 892-894; also social_plus_screen multiple sites; feed_post_card multiple sites
- **Est. reduction:** ~30 LOC across files, structural consistency | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The idiom ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(e.toString().replaceFirst('Exception: ', '')))) (and the no-arg 'Couldn't update — try again' variant) is hand-copied ~10+ times across the social partition. Every catch block re-implements the same error-to-snackbar mapping, so message/duration/styling drift between call sites (some pass duration, some don't).
- **Proposal:** Add a small shared helper, e.g. context.showErrorSnack(Object e, {String? fallback}) extension or a top-level showErrorSnack(messenger, e), that does the replaceFirst and standard duration. Replace the duplicated catch bodies with one call.
- **Verifier note:** Verified: the 4 race_hub sites (265, 312, 759, 893), 5 in social_feed_post_card, and 2 each in post_detail/user_profile_screen all match the exact snackbar idiom — 16 identical showSnackBar(SnackBar(Text(e.toString().replaceFirst('Exception: ','')))) instances across 7 files, plus a separate `_error =` state-assignment variant of the same replaceFirst (68 total replaceFirst calls app-wide). Drift is real: race_hub success snackbar carries a duration while the error one doesn't, and the fallback exists in inconsistent forms ("Couldn't update like/bookmark./Could not post comment."). A helper preserves behavior since every cited call site is already mounted-guarded; the top-level showErrorSnack(messenger, e) form is safest as some sites capture `messenger` before the await. One correction: severity 'low' is fair, but the finding under-scopes it to the social partition — the idiom spans ~39 files; ~30 LOC is a reasonable estimate for the snackbar variant alone.

### 62. [LOW] [duplication] Number formatting (whole-vs-decimal) reimplemented repeatedly
- **File:** `app/lib/screens/social/race_hub_screen.dart`
- **Where:** race_hub: _fmt (1718-1719), inline at 753, 1045-1047, 1187, 1192, 1241-1242; social_plus _generate (1469-1471); feed_post_card 589
- **Est. reduction:** ~15 LOC, single source of truth | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The pattern v % 1 == 0 ? v.toInt().toString() : v.toStringAsFixed(1) appears at least 7 times in race_hub_screen alone (one of them already named _RaceLogSheet._fmt) plus in social_plus and the feed card. It is the canonical 'drop the .0 on whole numbers' formatter copy-pasted instead of shared, so a rounding-policy change must be hunted across many sites.
- **Proposal:** Promote a single top-level/util formatWeightOrReps(double) (or extension on num) and use it everywhere — including _RaceLogSheet._fmt, the hero total at 1045-1047, _standingRow totalLabel, _heroStatusLine margins, and the feed card weight at 589.
- **Verifier note:** Confirmed all cited sites: race_hub_screen.dart 753, 1045-1047, 1187, 1192, 1241-1242, and the named _fmt at 1718-1719; social_plus_screen.dart 1469-1471; feed card at social_feed_post_card.dart:589 (finding's path was slightly wrong but line matches). The duplication is real and actually understated — the same 'drop the .0' pattern recurs ~16 times app-wide (workout_tracker, analytics, services, charts), so estReduction is conservative. Two equivalent spellings exist (v.toInt().toString() vs toStringAsFixed(0)) but both yield identical output for whole/one-decimal values, so a single util.formatWeightOrReps(double) preserves behavior. Severity 'low' is fair (cosmetic formatting, no correctness risk).

### 63. [LOW] [duplication] getDay's prefs-fallback duplicates _loadDayFromPrefsOnly verbatim
- **File:** `app/lib/services/nutrition_service.dart`
- **Where:** getDay (867-883) vs _loadDayFromPrefsOnly (811-829)
- **Est. reduction:** ~16 lines | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The bottom half of getDay (lines 867-883: read prefs key, jsonDecode, map entries, build DailyNutrition, catch+debugPrint) is a line-for-line copy of _loadDayFromPrefsOnly (811-829). Two copies of the same decode/guard logic that must be kept in sync (note getDay uses `j['waterMl'] as int?` while the server path uses `(num?).toInt()` — already a subtle inconsistency risk).
- **Proposal:** Replace the duplicated tail of getDay with `return _loadDayFromPrefsOnly(date);`.
- **Verifier note:** Confirmed: getDay's tail (867-883) is a verbatim copy of _loadDayFromPrefsOnly (811-829); both prefs blocks use `j['waterMl'] as int? ?? 0`. Replacing the tail with `return _loadDayFromPrefsOnly(date);` is behavior-preserving (only the debugPrint message text changes) and also drops an unused `prefs` local. Severity low is fair; est ~16 lines is accurate. One nit in the finding: the cited `as int?` vs `(num?).toInt()` inconsistency is NOT between these two blocks (both use `as int?`) — toInt() lives in the separate _dailyFromServerJson path, so that framing is slightly inaccurate but doesn't affect the core duplication claim.

### 64. [LOW] [duplication] _pickStr duplicated verbatim across ExerciseDto and ExerciseMediaDto
- **File:** `app/lib/services/workout_service.dart`
- **Where:** ExerciseDto._pickStr (957-961) and ExerciseMediaDto._pickStr (1026-1030)
- **Est. reduction:** ~6 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The camelCase/snake_case picker _pickStr is copy-pasted identically into two classes (and _stringList is a near-sibling helper only on ExerciseDto). Two exact copies of the same 5-line function.
- **Proposal:** Hoist _pickStr (and _stringList) to top-level private functions in the file and call them from both DTOs. Behavior-preserving.
- **Verifier note:** Confirmed: ExerciseDto._pickStr (957-961) and ExerciseMediaDto._pickStr (1026-1030) are byte-identical pure static helpers; _stringList (951-954) is only on ExerciseDto. Hoisting both to top-level file-private functions and updating call sites is behavior-preserving. Severity 'low' and ~6 LOC estimate are fair (the dup _pickStr is ~5 LOC; reuse of _stringList saves nothing since it's already single).

### 65. [LOW] [duplication] Month-name array + date formatter and pace m:ss formatter duplicated across widgets
- **File:** `app/lib/widgets/charts/personal_records_timeline.dart`
- **Where:** _formatDate + months[] (personal_records_timeline L306-310); identical months[] + _formatDate in activity_share_card.dart L357-363; _formatPace m:ss in splits_table.dart L172-177 and pace_chart.dart tooltip L101-104
- **Est. reduction:** ~25-30 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The 12-element month abbreviation array and a `months[m-1] day` formatter are written twice (personal_records_timeline.dart and activity_share_card.dart, plus a 13-element variant with leading '' in workout_consistency_heatmap.dart L284). The mm:ss pace formatting (`m = secs~/60; s=(secs%60).toInt(); padLeft(2,'0')`) is reimplemented in splits_table._formatPace and inline in pace_chart's tooltip getTooltipItem. These are small but the month table is genuinely copy-pasted 3 times.
- **Proposal:** Add shared format helpers (e.g. in a date_format util / existing formatters file): `String shortMonthDay(DateTime)` and `String paceMmSs(double secsPerKm)`, and reference from all sites. Removes 3 month-array copies and 2 pace-format copies.
- **Verifier note:** Duplication is real: the 12-element month array appears verbatim in personal_records_timeline.dart L307-308 and activity_share_card.dart L358-361, plus a 13-element 1-based variant in workout_consistency_heatmap.dart L284; the m:ss pace math is duplicated in splits_table._formatPace L172-177 and inline in pace_chart L101-104. But only the raw array/math is shared, not the formatting: outputs differ ("Mon Day" vs "Mon Day, Year" vs bare month name; pace adds "/km" suffix in one site and "--:--" guard in the other). The finder's single shortMonthDay(DateTime) helper cleanly fits only 1 of 3 month sites. A bare paceMmSs(double) and shortMonth name helper are behavior-preserving if call sites keep their year/suffix/guards. Severity "low" is correct, but the ~25-30 LOC estimate is overstated — net savings are ~10-15 LOC since a new util file adds lines and the 3 formatters can't fully collapse. Also note the cited paths for activity_share_card.dart and splits_table.dart are wrong (actual: app/lib/widgets/, not app/lib/widgets/charts/).

### 66. [LOW] [duplication] Bookmark-toggle logic duplicated in feed post card
- **File:** `app/lib/widgets/social_feed_post_card.dart`
- **Where:** _showPostMenu bookmark item (120-137) and action-row bookmark button (768-795)
- **Est. reduction:** ~20 LOC | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** The toggleBookmark flow — await service.toggleBookmark(postId), setState(_bookmarked = newState), onBookmarkChanged?.call(newState), catch -> show 'Couldn't update bookmark' snackbar — is written twice with the same body (the menu variant and the action-button variant differ only slightly in error text/reporting). Two copies means future changes (e.g. optimistic update like _handleLikeTap already does) must be made in both spots.
- **Proposal:** Extract a single Future<void> _toggleBookmark() method on the State and call it from both the menu item onTap and the action button onTap. Optionally mirror the optimistic+revert pattern already used by _handleLikeTap.
- **Verifier note:** Duplication is real but partial: the success path (toggleBookmark -> setState -> onBookmarkChanged) is identical in both blocks, but the catch blocks genuinely diverge — the menu variant (123-136) does NOT call reportError and surfaces the raw exception text, while the action button (776-793) calls reportError(e, st, ...) and shows a fixed string. A naive extract-and-call-from-both would change behavior in one site (lose telemetry or change snackbar copy) unless a unified error policy is chosen. Severity low is correct, but the ~20 LOC estimate is overstated — only ~3 identical success lines truly dedup; realistic net reduction is roughly 5-8 LOC.

### 67. [LOW] [duplication] ZActivityGrid duplicates its 5-step intensity color ramp in two places
- **File:** `app/lib/widgets/z/z_activity_grid.dart`
- **Where:** _cellColor (L64-77) and the inline `sample()` closure in ZActivityGridLegend (L135-148)
- **Est. reduction:** ~12 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The exact 5-case intensity->color switch (0=surface2, 1=brand 0.18, 2=0.40, 3=0.70, default=brand) is written twice: once as the instance method _cellColor in ZActivityGrid and again verbatim as a local `sample` function inside ZActivityGridLegend.build. Any future tweak to the ramp must be made in two files-sections in sync. Note the unrelated WorkoutConsistencyHeatmap uses a different 4-bucket ramp (_getHeatmapColor), so there are two competing heatmap color scales in the partition.
- **Proposal:** Hoist a single `static Color intensityColor(int v, Color brand)` (top-level or static on ZActivityGrid) and call it from both the grid and the legend. Optionally reconcile with WorkoutConsistencyHeatmap's scale if a unified look is desired.
- **Verifier note:** Confirmed: _cellColor (L64-77) and the sample() closure (L135-148) are byte-for-byte identical 5-case ramps (0=surface2, 1=0.18, 2=0.40, 3=0.70, default=brand), differing only in the brand variable name. Hoisting one static helper called from both is a pure, behavior-preserving refactor. The note about WorkoutConsistencyHeatmap._getHeatmapColor (L317-322) using a separate 4-bucket scale is also accurate and correctly flagged as optional. Severity (low) and ~12 LOC estimate are fair.

### 68. [LOW] [duplication] Four near-identical media-enrichment functions repeat the same exercise-spread mapping
- **File:** `backend/src/lib/exercise-media.ts`
- **Where:** buildMediaByExerciseId (73-82), enrichExerciseWithMedia (84-87), enrichWorkoutExerciseRow (91-97), enrichWorkoutWithExerciseMedia (103-113), enrichWorkoutsWithExerciseMedia (115-127)
- **Est. reduction:** ~15 LOC, structural | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** enrichWorkoutWithExerciseMedia and enrichWorkoutsWithExerciseMedia are byte-for-byte the same per-workout mapping body (the `{...we, exercise:{...we.exercise, media: mediaById.get(...)}}` block) — the single-workout version is just the plural version applied to one element. The `{ ...exercise, media }` shape is also re-spelled in enrichExerciseWithMedia and enrichWorkoutExerciseRow. Four functions encode one operation.
- **Proposal:** Implement enrichWorkoutWithExerciseMedia as `(await enrichWorkoutsWithExerciseMedia([w]))[0]` and factor a single `attachMedia(we, mediaById)` helper used by both the row and workout enrichers. Collapses two duplicated map bodies into one.
- **Verifier note:** Confirmed: the per-workout map body in enrichWorkoutWithExerciseMedia (lines 108-111) and enrichWorkoutsWithExerciseMedia (122-125) is byte-for-byte identical, and the singular is exactly the plural applied to [w] (flatMap of one workout yields the same exercise list; buildMediaByExerciseId dedups anyway), so (await enrichWorkoutsWithExerciseMedia([w]))[0] is behavior-preserving and [0] is always safe. A shared attachMedia(we, mediaById) helper cleanly dedupes the row enricher (93-96) plus both workout map bodies; note enrichExerciseWithMedia spreads at top level (different shape) so it is not covered by that helper, making it 3 call sites not 4. Severity low is correct; realistic net reduction ~8-12 LOC, slightly less than the claimed ~15.

### 69. [LOW] [duplication] Duplicated USDA proxy plumbing across two handlers
- **File:** `backend/src/routes/nutrition.ts`
- **Where:** POST /usda/foods/search (91-150) and GET /usda/food/:fdcId (153-201)
- **Est. reduction:** ~40 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The two USDA proxy endpoints repeat the same scaffolding: usdaApiKey() null-check + 503, fetch wrapped in try/catch -> 502 USDA_UPSTREAM, `await upstream.text()`, !upstream.ok -> status-mapped error with text.slice(0,500), JSON.parse with catch -> USDA_BAD_RESPONSE. Only the URL and method/body differ. ~50 lines are effectively duplicated.
- **Proposal:** Extract a `proxyUsda(reply, requestId, url, init?)` helper that does the fetch, status mapping, and JSON parsing, returning the parsed body or sending the appropriate error. Both handlers shrink to building the URL + calling it.
- **Verifier note:** Real duplication: lines 114-150 (POST) and 171-201 (GET) repeat fetch try/catch -> 502 USDA_UPSTREAM, await upstream.text(), !ok status-mapping with text.slice(0,500), and JSON.parse -> USDA_BAD_RESPONSE. Two minor diffs the finder omitted: POST has an extra `429 ? 429` branch in the status map (functionally redundant — fallthrough returns 429 anyway, so unifying is safe) and POST logs app.log.warn on fetch failure while GET does not. A proxyUsda helper preserves behavior as long as it keeps the warn-log; severity low is correct. estReduction ~40 LOC is slightly optimistic (helper itself ~25 LOC) but in the right range.

### 70. [LOW] [duplication] weekly-effort and weekly-sessions / daily-training duplicate the same volume aggregation SQL
- **File:** `backend/src/routes/stats.ts`
- **Where:** weekly-effort (12-27), daily-training vol CTE (63-74), top-exercises (130-150)
- **Est. reduction:** structural (correctness alignment + removes 3 copies of the fragment) | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** The identical join + aggregate fragment `INNER JOIN workout_exercises ... INNER JOIN workout_sets ws ON ... AND ws.tag = 'WORK' ... SUM(CAST(ws.weight_kg AS DOUBLE PRECISION) * ws.reps), COUNT(*) ... status IN ('completed','posted')` is hand-written in at least three raw queries with slightly different GROUP BY/time windows. The WORK-volume definition lives in 3+ places and can drift (e.g. one forgets isCompleted, which none of these filter — a latent inconsistency vs cumulative-volume/recent-prs which DO filter isCompleted=true and weightKg>0).
- **Proposal:** Note the actual inconsistency: the raw-SQL stats (weekly-effort/daily-training/top-exercises) sum ALL WORK sets regardless of isCompleted/weightKg, while cumulative-volume and recent-prs require isCompleted=true and weightKg>0. Decide on one volume definition and centralize it (a shared SQL fragment constant or a SQL view). At minimum align the WHERE clauses so 'volume' means the same thing across endpoints.
- **Verifier note:** Both claims verified in backend/src/routes/stats.ts: the WORK-volume join+SUM(CAST(ws.weight_kg AS DOUBLE PRECISION)*ws.reps)+COUNT(*)+status IN('completed','posted') fragment is hand-copied across weekly-effort (16-21), daily-training vol CTE (64-72), and top-exercises (135-143). The inconsistency is real: those three raw queries filter only tag='WORK', while cumulative-volume (295-298) and recent-prs (348-350) additionally require isCompleted:true and weightKg>0, so 'volume' silently differs (planned/zero-weight sets count in one set of endpoints but not the other). Aligning the WHERE clauses is a deliberate correctness change, NOT behavior-preserving; a pure shared-fragment extraction would preserve behavior but leaves the divergence. Severity low is fair, leaning low-medium since hero-chart numbers won't reconcile with weekly/daily.

### 71. [LOW] [perf] _maxVol() recomputed every build in WeeklyEffortScreen chart
- **File:** `app/lib/screens/analytics/weekly_effort_screen.dart`
- **Where:** build() -> BarChart: horizontalInterval _maxVol()/4 and maxY _maxVol()*1.15 (lines 75, 120, 130-133)
- **Est. reduction:** structural (1 list-scan removed per build) | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** `_maxVol()` walks the full points list with `map().reduce()` and is called twice within a single BarChartData construction (once for horizontalInterval, once for maxY). It runs on every rebuild even though _points only changes on _load. Cheap at 12 points, but it is recomputation in the build hot path and the pattern is copied into the (likely-dead) analytics_hub builders too.
- **Proposal:** Compute `final maxVol = _maxVol();` once at the top of build (or store it in state alongside _points after _load) and reuse it for both interval and maxY.
- **Verifier note:** Confirmed: _maxVol() (lines 130-133) walks _points via map().reduce() and is called twice in one BarChartData build (line 75 horizontalInterval, line 120 maxY). It's a pure function of _points with no side effects, so hoisting to a single `final maxVol = _maxVol();` is behavior-preserving. Severity 'low' is correct — max 12 points, negligible cost.

### 72. [LOW] [perf] XP completion subtree rebuilt every animation frame
- **File:** `app/lib/screens/workouts/xp_complete_screen.dart`
- **Where:** _XpCompleteScreenState.build, AnimatedBuilder at lines 247-353
- **Est. reduction:** structural (one content build vs ~48 during animation) | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The AnimatedBuilder for the entrance scale/opacity (800ms elastic) wraps the ENTIRE content column as its builder return value — the success badge, XP text, age chip, coach insight card, gameXp line, full XP breakdown list, and the ranks section. The builder ignores the `child` param, so this whole tree is reconstructed ~60 times/sec for the animation duration even though only Opacity and Transform.scale actually change per frame. The ranks/breakdown subtrees are static relative to the animation.
- **Proposal:** Pass the static content Column as AnimatedBuilder's `child:` argument and have the builder return Opacity(opacity:..., child: Transform.scale(scale:..., child: child)). The content then builds once; only the two transform wrappers update per frame. Behavior identical.
- **Verifier note:** Confirmed: AnimatedBuilder at lines 247-353 wraps the entire content Column in its builder and ignores the `child` param, so the whole subtree rebuilds each frame of the 800ms elasticOut animation while only _opacity/_scale change. Proposed fix (hoist Column into `child:`, return Opacity(Transform.scale(child:child))) is the standard idiom and behavior-preserving. Severity "low" is right — rebuilt nodes are mostly const/Text and the subtree already gets setState rebuilds from async insight/rank loads anyway; ~48-frame estimate is reasonable.

### 73. [LOW] [perf] worldRecordByName does an O(n) scan with per-key normalize on every spec resolve
- **File:** `backend/src/services/gym-xp.service.ts`
- **Where:** worldRecordByName (79-85) called from resolveSpec (87-116)
- **Est. reduction:** O(n) scan → O(1) per lookup; structural | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** `resolveSpec` is called once per exercise in `computeWorkoutGameXp` (line 226). For exercises not in NAME_ALIASES it calls `worldRecordByName`, which iterates ALL WORLD_RECORDS entries and runs `normalizeName` (trim+toLowerCase) on every key for each lookup (lines 80-83). The WORLD_RECORDS keys are static, so the normalized form is recomputed on every call instead of once at module load.
- **Proposal:** Build a `Map<normalizedName, WrSpec>` once at module init from Object.entries(WORLD_RECORDS), then `worldRecordByName` becomes a single Map.get(normalizeName(name)). Eliminates the per-call linear scan and repeated key normalization.
- **Verifier note:** Accurate: worldRecordByName (lines 79-85) does scan all 13 WORLD_RECORDS entries and re-runs normalizeName on every key per call, invoked once per exercise via resolveSpec line 93 / computeWorkoutGameXp line 226. The Map-at-init fix preserves behavior (all 13 keys are collision-free after normalize) PROVIDED the defensive copy is kept — Map.get must return { ...spec } like the current code, not a shared reference. Severity 'low' is if anything generous: n is a fixed 13 and only a handful of exercises per workout, so the real-world cost is negligible; this is a micro/structural cleanup, not a meaningful perf win.

### 74. [LOW] [redundant-io] ModerationService.getCachedBlockedIds re-read from SharedPreferences on every feed/comment fetch
- **File:** `C:\proiect\redesign\app\lib\services\moderation_service.dart`
- **Where:** getCachedBlockedIds (184-192); callers in social_feed_service.dart (3×: getFeedPage, getBookmarks, getComments) and social_story_service.dart loadActiveStories (289)
- **Est. reduction:** structural (eliminates 1 disk read per feed page / comment open) | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Every paginated feed page, every bookmarks page, every comments open, and every story-feed load calls getCachedBlockedIds(), which does a fresh `SharedPreferences.getInstance()` + getStringList + toSet on each call. During scroll-driven pagination this re-parses the same block list from disk repeatedly. The list changes only on block/unblock/syncCacheFrom, so re-reading per page is wasted IO on a scroll hot path.
- **Proposal:** Cache the parsed Set<String> in a static field, populated lazily on first read and invalidated inside _addToLocalCache/_removeFromLocalCache/syncCacheFrom/clearLocalCache (all already write paths in this same class). getCachedBlockedIds returns the in-memory set. Keeps the SharedPreferences value as the source of truth across launches while removing per-page disk reads.
- **Verifier note:** Real but cost overstated. getCachedBlockedIds (lines 184-192) is indeed called per feed page (social_feed_service.dart:118,190,302), per story load (social_story_service.dart:289), and also friends_service.dart:110 (finding missed this one); each call does getStringList+toSet. However SharedPreferences loads its entire store into an in-memory map on first getInstance() and caches it, so this is NOT per-page disk IO — it's an in-memory map lookup plus a small list/Set allocation. So 'redundant-io'/'eliminates 1 disk read per page' is inaccurate; the savings are a tiny allocation, confirming severity:low at best. The static-cache proposal is behavior-preserving and actually the right shape since callers use fresh ModerationService() instances (story/friends) rather than a shared instance, and all four write paths (_add/_remove/syncCacheFrom/clearLocalCache) are present in this class to invalidate it. Net: a reasonable micro-cleanup, not a meaningful IO win.

### 75. [LOW] [redundant-io] Redundant full onboarding sync can run twice on the finish path
- **File:** `app/lib/screens/onboarding/onboarding_v3.dart`
- **Where:** _OnboardingV3State._finish (l.220-260) together with _ScrBuildingState._start (l.1610-1670)
- **Est. reduction:** up to 1 duplicate profile+settings PATCH and 1 duplicate AI plan call per slow finish | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** _ScrBuilding kicks the real OnboardingService().completeOnboarding (profile PATCH + settings PATCH + AI weekly-plan) at screen 10 and stores it in data.syncFuture, guarding re-entry. _finish then awaits that future with an 8s timeout; if the future hasn't resolved in time (slow DeepSeek plan call is expected — that's the whole 'AI early' rationale) the timeout swallows it and `!_data.synced` triggers a SECOND completeOnboarding() — re-issuing both PATCHes and another AI plan generation even though the first is still in flight and will likely succeed. On a slow network this doubles the write/AI load right at launch.
- **Proposal:** Before the fallback completeOnboarding in _finish, check whether syncFuture is still pending (e.g. track a `bool _syncInFlight` set in _ScrBuilding, or have OnboardingService expose/await the existing future without a hard timeout when one exists) and only fire a fresh sync if the prior attempt actually errored/was never started. The 8s-timeout-then-resync currently treats 'still working' the same as 'failed'.
- **Verifier note:** Confirmed real. In _finish (onboarding_v3.dart l.227-249), the in-flight syncFuture is awaited with f.timeout(8s); on timeout the catch swallows it and _data.synced stays false (it is only set in the future's .then at l.1658 or from r at l.231), so !_data.synced fires a second full completeOnboarding. That re-issues both PATCHes (profile l.376, settings l.423) and a fresh DeepSeek plan call (l.480) — the prewarm cache is nulled on the first run (l.476-477), so the duplicate is a real new AI call, not a cheap cache hit. DeepSeek plan generation routinely exceeds 8s (the file's own comments cite 30-120s), so the duplicate is likely on slow networks. There is no server-side idempotency key, so the writes truly double. Severity 'low' is fair (best-effort path, user data converges, no correctness break — just wasted writes/AI cost). Proposal (track _syncInFlight to distinguish 'still working' from 'failed/never-started') is behavior-preserving; only caveat: a never-resolving hung future could then never fall back, so any fix should still cap total wait or only resync on observed error.

### 76. [LOW] [redundant-io] PersonalRecordsTimeline sorts and re-takes the record list three times (already pre-sorted)
- **File:** `app/lib/widgets/charts/personal_records_timeline.dart`
- **Where:** _prMilestonesFromProgressions L79-80 then _buildTimeline L168-171
- **Est. reduction:** ~5 LOC + per-build sort removed | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** _prMilestonesFromProgressions already sorts rows descending by date and does `.take(25)` before storing into _records. Then _buildTimeline re-sorts the same list descending again (L168 `List.from(_records)..sort`), takes 20, then `.reversed.toList()` — so the list is sorted twice and copied 3 times per build. The second sort is redundant because _records is already date-desc. Header text also says '${_records.length} PR milestones' while only 20 render, a minor inconsistency.
- **Proposal:** Drop the re-sort in _buildTimeline (use _records directly), compute `recentPRs` once. Since this runs in build(), also consider caching recentPRs in _load. Removes one O(n log n) sort + two list copies per frame.
- **Verifier note:** Confirmed: _prMilestonesFromProgressions sorts date-desc and take(25) into _records (L79-80), then _buildTimeline re-sorts with the identical comparator (L168) before take(20).reversed (L171). The re-sort is redundant since _records is already date-desc, and using _records directly preserves output exactly (same comparator = same tie handling). Severity 'low' is correct — the list is capped at 25 items so the per-build sort cost is negligible; this is a cleanliness win, not a perf one. Minor correction: only ~2 list copies occur per build (List.from + reversed.toList), not 3 — the take(25).toList copy happens once in _load. The header '_records.length PR milestones' vs 20 rendered is a real but separate pre-existing inconsistency.

### 77. [LOW] [redundant-io] Single-item media enrichers issue one DB query per call (N+1 if ever looped)
- **File:** `backend/src/lib/exercise-media.ts`
- **Where:** enrichExerciseWithMedia (84-87) and enrichWorkoutExerciseRow (91-97) — each calls buildMediaByExerciseId([single])
- **Est. reduction:** prevents future N+1; structural | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Each single-item helper wraps its one exercise in an array and runs the full lookupGifUrls DB round-trip (a Postgres pool query against the external media DB). Used at exercises.ts:148/266 (custom-exercise create/update — single, fine) and workouts.ts:267 (single workoutExercise add). These are currently 1-per-request so not an active N+1, but the API shape invites a caller to map over a list and trigger one media-DB query per item. The bulk path buildMediaByExerciseId already batches via `= ANY($1)`, so the risk is purely from these convenience wrappers.
- **Proposal:** Keep the wrappers but document/guard that list callers must use the bulk enrichers; or have the single-item wrappers delegate to a shared request-scoped batch. Low priority since current call sites are singular — flag to prevent future misuse.
- **Verifier note:** Confirmed: enrichExerciseWithMedia (84-87) and enrichWorkoutExerciseRow (91-97) each wrap one exercise and run lookupGifUrls' single DB query; all three call sites (exercises.ts:148,266; workouts.ts:267) are genuinely singular, so there is no active N+1. Severity "low" is right but generous — this is a speculative/preventive note, not a current inefficiency (one query for one item is not redundant work). The proposal is structural, preserves behavior, and yields zero current reduction; "prevents future N+1" is the only honest benefit.

### 78. [LOW] [redundant-io] Owner lookup is an extra sequential query after like/comment write
- **File:** `backend/src/routes/posts.ts`
- **Where:** POST /:id/likes (358-361) and POST /:id/comments (400-403)
- **Est. reduction:** 1 query per like/comment | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** Both handlers create the like/comment, then issue a separate prisma.post.findUnique to fetch the owner userId for the notification. The post row is needed but never fetched up front, so on the like path there are up to 4 sequential round-trips (findUnique like, delete/create, findUnique owner, create analytics). The owner id could be obtained without a second post read.
- **Proposal:** Fetch the post owner once at the top (select userId) and validate existence there, reusing it for both the write and the notification. Alternatively select post.userId via the like/comment create's return relation. Saves one round-trip per like/comment.
- **Verifier note:** Confirmed: both handlers (likes 358-361, comments 400-403) issue a separate post.findUnique after the write solely to get owner.userId for the notification; the like create path is indeed 4 sequential round-trips. Estimate (1 query each) and 'low' severity are correct. The behavior-preserving fix is the second proposal — select the post.userId relation off the create's return — which removes the read in the same round-trip; the first proposal ('fetch owner at top and validate existence') is NOT behavior-preserving because neither handler currently 404s on a missing post (it would hit an FK error/500), so adding that check changes behavior.

### 79. [LOW] [simplification] Per-metric loaders in TrainingMetricDetail repeat the same reduce/fold boilerplate
- **File:** `app/lib/screens/analytics/training_metric_detail_screen.dart`
- **Where:** _loadVolume/_loadStrength/_loadExercises/_loadConsistency/_loadSteps/_loadCalories (173-552)
- **Est. reduction:** ~70 LOC | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** Each of the six loaders independently recomputes max via `pts.map(x).reduce((a,b)=>a>b?a:b)`, total via `fold`, and a normalized chart series `[for p in pts) max>0 ? v/max*100 : 0]` plus the weekday-initials array `const days=['M','T','W','T','F','S','S']` (duplicated in _loadSteps and _loadCalories). _loadSteps and _loadCalories are structurally identical except the accessor (steps vs caloriesBurned), unit strings, and thresholds (10000 vs 600).
- **Proposal:** Extract a `_normalizedBars(values)` helper and a single `_dailyWearableMetric({values, unit, label, target, insightHigh, insightLow})` builder that _loadSteps and _loadCalories both call. Hoist the weekday-initials constant. Collapses two ~55-line methods into one plus thin wrappers and removes the repeated max/total idioms.
- **Verifier note:** The Steps/Calories duplication is real: both loaders (439-552) share the same max-via-reduce, total-via-fold, normalized `max>0?v/max*100:0` bar series, and a duplicated `const days=['M','T','W','T','F','S','S']`. But the finding's "all six loaders" framing is overstated — _loadStrength uses ZveltAreaChart with raw points (no normalized bar series, no max-reduce) and _loadExercises has no chart at all, so the shared idiom really only applies to Volume/Consistency/Steps/Calories. The proposed builder is behavior-preserving but needs more params than listed: steps is int vs calories double (affects total's type), and the two diverge in value formatting (_thousands vs .round()) plus most stat/insight strings, so the wrappers won't be as thin as claimed. Realistic saving is ~40-50 LOC, not ~70; severity low is correct.

### 80. [LOW] [simplification] defaultRepsFromRepRange time-detection uses overlapping/over-specific substring checks
- **File:** `backend/src/lib/rep-range.ts`
- **Where:** defaultRepsFromRepRange (rep-range.ts:5-33), conditions lines 8-13
- **Est. reduction:** ~6 LOC, removes magic-string coupling | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** The time/conditioning guard tests five overlapping conditions including `lower.includes('m ')` AND a redundant `lower.includes('40m')` (40m already contains 'm' but not 'm ' — still, it's a hardcoded magic token from one blueprint repRange '40m / rounds'). The `/\d+\s*s\b/` + 'sec' overlap, and 'm ' is fragile (matches 'arm ' style strings). This is a branchier-than-needed classifier driven by specific blueprint strings rather than a pattern.
- **Proposal:** Replace the five ad-hoc includes with one regex covering duration/conditioning units: `/\b\d+\s*(s|sec|secs|seconds|m|min)\b|hold|round/i.test(lower)`. Drops the hardcoded '40m' literal and the fragile 'm ' check while keeping the same time→1 behavior.
- **Verifier note:** Confirmed at rep-range.ts:7-14: the guard really uses 5 overlapping/over-specific includes, with '40m' a hardcoded magic literal from the '40m / rounds' blueprint and 'm ' genuinely fragile (I verified it misclassifies 'arm circles' -> 1). On the actual blueprint strings ('8–12', '8–12 / leg', '6–8 / leg', '40m / rounds', '30–60s hold', etc.) old and proposed regex produce identical results, so the rename is safe for current canned inputs. BUT repRange is also fed free-form LLM output (sanitizeRepRange, sliced to 16 chars), and the proposal is NOT strictly behavior-preserving for edge inputs: 'arm circles' goes 1->8 and '2 min' goes 2->1. Both changes are actually corrections (old results were the bug), and no tests cover this function, but it is a behavior change, not a pure refactor. Severity 'low' is fair; est ~6 LOC is accurate.

### 81. [LOW] [simplification] selectBlueprint scoring double-counts split match and uses redundant tiebreak
- **File:** `backend/src/programming/blueprints.ts`
- **Where:** selectBlueprint (blueprints.ts:123-168), scoring block 148-149 and sort 159-164
- **Est. reduction:** ~6 LOC, removes a double-scoring branch | **Behavior-preserving:** NO — see note | **Verify confidence:** high
- **Problem:** Lines 148-149 add +3 when `splitPreferences includes split OR includes 'auto'`, then +5 again when `split!=='auto' && includes split` — a blueprint that lists the exact split gets +3 (it also has 'auto') then +5, while one that only has 'auto' gets +3; the branching is hard to read and the 'auto'-always-present convention means the first clause is almost always true. Separately, the secondary sort (159-164) re-derives the wildcard penalty already encoded as `score -= 1` at line 143, so the tiebreaker partly duplicates the primary score signal.
- **Proposal:** Flatten to explicit additive terms: `if (b.splitPreferences.includes(split)) score += 8; else if (b.splitPreferences.includes('auto')) score += 3;`. Drop the wildcard tiebreak (lines 161-163) since the `-1` wildcard penalty at 143 already orders specific blueprints ahead of the fallback. Behavior-preserving for the documented inputs and easier to reason about.
- **Verifier note:** Split half is real but mischaracterized: every blueprint includes 'auto', so line 148 is a constant +3, and 148+149 sum correctly to +8/+3 (not a buggy double-count). The proposed flatten changes the split==='auto' case from +3 to +8, but since that's a constant across all blueprints it preserves the *selected* blueprint (ordering-preserving, not score-preserving). The tiebreak half is wrong: the -1 wildcard penalty only orders the fallback last when all other terms are equal; the secondary sort additionally breaks ties where a specific blueprint and the wildcard reach an equal total via different terms, so it is NOT redundant and dropping it can flip the winner. selectBlueprint has no callers in-repo, so impact is low; severity 'low' is correct but est ~6 LOC is optimistic since the tiebreak must be kept (~1 LOC net).

### 82. [LOW] [simplification] Calendar day-bucket initialization repeated and date-range filter is brittle string compare
- **File:** `backend/src/routes/activities.ts`
- **Where:** GET /v1/activities/calendar, lines 117-169
- **Est. reduction:** ~10 LOC, removes shape drift | **Behavior-preserving:** yes | **Verify confidence:** high
- **Problem:** The `if (!days[key]) days[key] = { types:[], workoutIds:[], planned:[] ...}` initializer is written 3 times (lines 129, 142, 161) with slightly different shapes (the workout loop omits the `nutrition: undefined` the other two include), which is an inconsistency waiting to bite. Separately, planned and nutrition queries filter days with `day: { gte: month+'-01', lte: month+'-31' }` (lines 137, 156) — a lexical string range that includes nonexistent dates like '2026-02-31'; harmless today only because `day` values are themselves valid, but it's a fragile pattern next to the precise UTC bounds used for workouts.
- **Proposal:** Add a local `ensureDay(key)` closure returning/initializing the bucket with one canonical shape, and call it in all three loops. Optionally compute the real last-of-month (`new Date(Date.UTC(y,mo,0)).getUTCDate()`) for the planned/nutrition `lte` instead of the hardcoded '-31'.
- **Verifier note:** Confirmed: bucket initializer repeated 3x (lines 129, 142, 161), with line 129 omitting `nutrition: undefined` (drift is benign since undefined == absent in the JSON response, but real). The `lte: month + '-31'` lexical range at lines 137/156 is genuinely brittle next to the precise UTC bounds used for workouts at 101-105. An ensureDay() closure and a computed last-of-month are both behavior-preserving. Severity low is correct; LOC saving is closer to ~6-8 than 10.


## Rejected by verification

- **[low] TrainingProfileService().fetch() called from 4 independent code paths per Home load** (`app/lib/screens/skeleton/skeleton_home_tab.dart`) — fetch() IS memoized: it's a read-through cache backed by SharedPreferences (training_profile_service.dart:36-55, app_data_cache.dart:108-115). On a warm cache fetch(refresh:false) returns from disk with NO network call, and the first network fetch saves to disk so later same-load callers hit disk. So steady-state Home opens issue 0 profile GETs (not 1+); _loadTrainingHistory only forces network when refresh:true (pull-to-refresh). The only genuine duplication is a narrow cold-cache race on first-ever launch where _loadTrainingHistory and _CoachTipCard._load can both GET before either writes the cache. _openGoalEvolution (391) is a user-initiated flow, not part of the Home render path, so counting it is wrong. The hoist-and-pass-down proposal is behavior-preserving and removes the cold-launch race + redundant disk reads, but the cited estimate ("1+ network GET per Home open") is overstated; low severity is, if anything, generous.
- **[low] StrengthAnalyticsScreen awaits two independent fetches sequentially** (`app/lib/screens/analytics/strength_analytics_screen.dart`) — At lines 39-42 both futures are started before either is awaited, so they already run concurrently (the finder concedes "both are in flight"); the Future.wait change is cosmetic-only with zero perf gain, and the serialization risk is hypothetical. The "more important" claim of no shared cache is wrong: getWeeklyEffort routes through _cachedListData and getDailyTraining through AppDataCache, both cache-first (2h TTL, stale-on-error fallback), so re-opening a screen within TTL does not re-hit the endpoint. The only genuine minor inefficiency is that distinct windows (8w/12w/6w/30d/...) are cached under separate keys rather than derived from one superset series — but that is a different, harder change than the proposal, which is already implemented. estReduction of "1 redundant request per screen open" is unsupported.
- **[high] Redundant N+1 participant-count fetch in Race Hub** (`app/lib/screens/social/race_hub_screen.dart`) — Finding's premise is false: the /challenges/feed endpoint's serializeChallenge (backend/src/routes/challenges.ts:46-82) does NOT emit participantsCount, so the model (social_challenge.dart:144) always deserializes it to 0. Thus race_hub line 126 hydrates 0 for every challenge, and _loadParticipantCounts() (the N calls to /participants, which returns real total=data.length) is the ONLY source of true counts. Dropping it — or the 'only fetch when null/absent' variant (the field is 0, never null) — would regress all counts to 0. The N calls are real, but eliminating them requires a backend change (add _count to the feed query), which is the opposite of the proposal as written; severity should be downgraded since the redundancy described does not exist.